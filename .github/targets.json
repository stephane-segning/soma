{
  "$comment": "Single source of (os, arch) truth for the server release workflow. Read at job start via jq and consumed as a matrix include via fromJSON. Adding a new target = adding one entry here; release-server.yml picks it up automatically (server matrix is linux-only by definition). The desktop is the Tauri app under desktop/desktop-app; its release pipeline is not yet wired (see PR removing the legacy Electron app).",
  "server": [
    {
      "arch": "amd64",
      "runs_on": "ubuntu-latest",
      "rust_target": "x86_64-unknown-linux-musl",
      "linker_env": "CARGO_TARGET_X86_64_UNKNOWN_LINUX_MUSL_LINKER"
    },
    {
      "arch": "arm64",
      "runs_on": "ubuntu-24.04-arm",
      "rust_target": "aarch64-unknown-linux-musl",
      "linker_env": "CARGO_TARGET_AARCH64_UNKNOWN_LINUX_MUSL_LINKER"
    }
  ]
}
