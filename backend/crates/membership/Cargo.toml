[package]
name = "soma-membership"
version.workspace = true
edition.workspace = true
publish.workspace = true
license.workspace = true
authors.workspace = true

[dependencies]
soma-core = { workspace = true }
soma-common = { workspace = true }
soma-peer = { workspace = true }
soma-proto-build = { workspace = true }
soma-storage = { workspace = true }

async-trait = { workspace = true }
base64 = { workspace = true }
libp2p = { workspace = true }
prost = { workspace = true }
prost-types = { workspace = true }
rand = { workspace = true }
tracing = { workspace = true }
tokio = { workspace = true, features = ["sync"] }

[dev-dependencies]
# Only needed to spell `RepositoryProvider::pool()`'s return type in
# `test_support::FakeRepositoryProvider` — the fake never actually opens a
# connection.
sqlx-utils = { workspace = true }
