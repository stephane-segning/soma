target/
node_modules/
dist/
*.log
.DS_Store

out
build
.idea

.sock
.pnpm-store

# Local blob storage pools (runtime data, not source).
backend/blobs/
backend/blobs-test/

*.db
data
.data

site
artifacts

# desktop/desktop-app/src-tauri/gen/{apple,android} (Tauri iOS/Android
# scaffolding) IS tracked — it's a real Xcode/Gradle project, not a build
# byproduct. Each has its own nested .gitignore for the genuinely-derived
# artifacts inside it (Pods/, build/, .gradle, local.properties, etc.).

storybook-static

# Vitest / Playwright outputs
coverage
playwright-report
test-results
.features-gen

# Local Claude Code agent state (per-checkout, not source).
.claude/