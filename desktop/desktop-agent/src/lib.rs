//! In-process agent runtime + chat/embed/rerank client. Replaces the
//! agent half of `addon-runtime.ts` plus `agent-client.ts` + `agent-config.ts`.
//! Phase 1 carries the lifecycle wrapper only.

pub mod runtime;
