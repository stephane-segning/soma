# Soma Release Bundle

This bundle pins:
- `soma-daemon`: {{version}}
- `soma-agentd`: {{version}}
- `soma` desktop: {{desktop_version}}
- `tapia` desktop: {{desktop_version}}

## Install

Run:

```bash
./install.sh
```

## Uninstall

Run:

```bash
./uninstall.sh
```

For more docs, see: {{docs_url}}

## Docker images

{{docker_images}}
