# Soma Release Bundle

This bundle pins:
- `soma-daemon`: {{version}}
- `soma-agentd`: {{version}}
- `soma` desktop: {{desktop_version}}

Source releases:
- daemons: `{{daemons_repo}}` @ `{{daemons_tag}}`
- desktop: `{{desktop_repo}}` @ `{{desktop_tag}}`
- daemons manifest: `{{daemons_manifest_source}}`
- desktop manifest: `{{desktop_manifest_source}}`

## Install

Run:

```bash
./install.sh
```

## Uninstall

Run:

```bash
./uninstall.sh
```

For more docs, see: {{docs_url}}

## Docker images

{{docker_images}}
