{
	"$schema": "../gen/schemas/desktop-schema.json",
	"identifier": "desktop",
	"description": "Desktop-only capability: plugins with no Android/iOS support (process, window-state, updater) or that back the custom titlebar / OS window chrome.",
	"windows": ["main"],
	"platforms": ["macOS", "windows", "linux"],
	"permissions": ["process:default", "window-state:default", "updater:default"]
}
