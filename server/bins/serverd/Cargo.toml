[package]
name = "soma-serverd"
version = "0.1.0"
edition = "2021"

[dependencies]
soma-rendezvous = { path = "../../crates/rendezvous" }
soma-relay = { path = "../../crates/relay" }
soma-server-api = { path = "../../crates/api" }

