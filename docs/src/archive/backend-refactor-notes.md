# Archive: Backend Refactor Notes (Daemon/Bot/BFF)

This page is kept for historical reference only.

It summarizes an earlier integration/refactor phase when several backend pieces moved from skeleton state into working implementations. Current architecture docs should be treated as the source of truth instead.

- Original subject matter: daemon IPC over UDS, join flow, relay client wiring, bot control plane, BFF wiring, peer event dispatching, and builder cleanup.
- If any behavior here conflicts with current docs or current code, prefer current docs and current code.
