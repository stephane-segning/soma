import{_ as t,o,c as s,a2 as a}from"./chunks/framework.BDCokyix.js";const u=JSON.parse('{"title":"@soma/ui Audit — Storybook reality check","description":"","frontmatter":{},"headers":[],"relativePath":"architecture/prd/ui-revamp-v0-audit.md","filePath":"architecture/prd/ui-revamp-v0-audit.md","lastUpdated":1780254295000}'),d={name:"architecture/prd/ui-revamp-v0-audit.md"};function r(i,e,n,c,l,h){return o(),s("div",null,[...e[0]||(e[0]=[a(`<h1 id="soma-ui-audit-—-storybook-reality-check" tabindex="-1"><code>@soma/ui</code> Audit — Storybook reality check <a class="header-anchor" href="#soma-ui-audit-—-storybook-reality-check" aria-label="Permalink to &quot;\`@soma/ui\` Audit — Storybook reality check&quot;">​</a></h1><p>Companion to <a href="./ui-revamp-v0-scaffold">ui-revamp-v0-scaffold.md</a>. The user flagged that &quot;most components in <code>@soma/ui</code> are broken.&quot; This audit walked every Storybook story in <a href="./../../../../desktop/desktop-ui/src/stories">desktop/desktop-ui/src/stories/</a> — 7 story files, 17 stories total — and identifies the root causes so the scaffold plan can give honest refactor-vs-rebuild verdicts.</p><p>Verdict in one sentence: <strong>two root causes account for almost everything visible as &quot;broken&quot;</strong>, plus three components that work and form an acceptable starting point. The scaffold plan&#39;s refactor-targets section needs corrections accordingly.</p><h2 id="stories-visited" tabindex="-1">Stories visited <a class="header-anchor" href="#stories-visited" aria-label="Permalink to &quot;Stories visited&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Story</th><th>Verdict</th><th>Notes</th></tr></thead><tbody><tr><td><code>Chat / AI Conversation</code> (<code>with-thinking</code>, <code>tools-and-sources</code>, <code>streaming-thinking</code>)</td><td><strong>Works</strong></td><td>Roles, expandable thinking card, tool + source headers, scrollback, composer with attach / mic / model chip render. Mock content uses stale terms (e.g. &quot;Install daemon&quot;) — content, not component bug.</td></tr><tr><td><code>Inputs / AI Input</code> (<code>default</code>, <code>with-preset</code>)</td><td><strong>Theme-broken</strong></td><td>Dark navy story background, dark text from <code>cmyk</code> theme → unreadable. Component code is fine; misconfigured story decorator (root cause #2 below).</td></tr><tr><td><code>Inputs / AI Model Selector</code> (<code>default</code>)</td><td><strong>Theme-broken + clipped</strong></td><td>Same dark-on-dark; the open dropdown floats off the left edge of the story canvas.</td></tr><tr><td><code>Inputs / PolymorphButton</code> (<code>variants</code>, <code>sizes</code>, <code>icon-only</code>, <code>loading</code>)</td><td><strong>Code-broken</strong></td><td>Every textual variant renders as a circle with overflowing label (&quot;Primar&quot;, &quot;Secondar&quot;, &quot;Subtle&quot; invisible). Root cause #1.</td></tr><tr><td><code>Desktop / Shell</code> (<code>basic</code>, <code>with-sidebars</code>, <code>with-header-and-footer</code>, <code>scrollable-content</code>, <code>persistent-widths</code>)</td><td><strong>Works (incomplete)</strong></td><td>Three-column layout with nav rail, main, status rail renders. No 52px icon rail or right panel-stack yet — but the foundation is the closest extant precedent for the four-column shell.</td></tr><tr><td><code>Desktop / Overlays</code> (<code>overlay-showcase</code>)</td><td><strong>Mixed</strong></td><td>Modal opens, content readable. <em>But</em>: trigger buttons are circles with truncated text (root cause #1), and the modal itself uses banned <code>shadow-2xl</code> + <code>rounded-2xl</code> via the <code>glass-panel</code> / <code>surface-card</code> utilities in styles.css.</td></tr><tr><td><code>Tapia / Char Display</code> (<code>default</code>, <code>live-typing</code>)</td><td><strong>Works</strong></td><td>Matches the locked design. Color-coded grapheme diff renders correctly. Density acceptable.</td></tr></tbody></table><p>Stories that <strong>don&#39;t exist</strong> for components my scaffold plan named as refactor targets:</p><ul><li><code>overlays/command-palette.tsx</code> — no story; behavior unverified.</li><li><code>overlays/bubble-toolbar.tsx</code> — no story; behavior unverified.</li><li><code>overlays/context-menu.tsx</code> — no story.</li><li><code>overlays/notification-drawer.tsx</code> — no story.</li><li><code>lists/roster-item.tsx</code> — no story.</li><li><code>forms/ai-model-selector.tsx</code> — has a story, but the story is broken (above).</li><li><code>presence/status-badge.tsx</code> — no story.</li><li><code>cards/launcher-card.tsx</code> — no story.</li><li><code>layout/window-chrome.tsx</code> — no story.</li></ul><p>So the &quot;starting points&quot; the scaffold leaned on are mostly unverified. Treat them as code-with-types-and-shape, not as proven implementations.</p><h2 id="root-cause-1-—-polymorphbutton-defaults-to-shape-circle" tabindex="-1">Root cause #1 — <code>PolymorphButton</code> defaults to <code>shape=&quot;circle&quot;</code> <a class="header-anchor" href="#root-cause-1-—-polymorphbutton-defaults-to-shape-circle" aria-label="Permalink to &quot;Root cause #1 — \`PolymorphButton\` defaults to \`shape=&quot;circle&quot;\`&quot;">​</a></h2><p><a href="./../../../../desktop/desktop-ui/src/components/actions/polymorph-button.tsx">desktop/desktop-ui/src/components/actions/polymorph-button.tsx:54</a> declares <code>shape = &quot;circle&quot;</code> as the default. DaisyUI&#39;s <code>btn-circle</code> constrains the button to a fixed-size disc — fine for icon-only triggers, wrong for labelled buttons. Every consumer that doesn&#39;t pass <code>shape=&quot;default&quot;</code> ends up with a circle plus overflowing label text. This is what makes <code>PolymorphButton / Variants</code>, the Overlays trigger buttons, and the modal&#39;s <code>Save</code> button look broken.</p><p><strong>Fix.</strong> Flip the default to <code>shape=&quot;default&quot;</code>. Callers that actually want a disc keep their explicit <code>shape=&quot;circle&quot;</code>. Wave 1 scope.</p><h2 id="root-cause-2-—-storybook-decorator-s-dark-background-fights-the-daisyui-light-theme" tabindex="-1">Root cause #2 — Storybook decorator&#39;s dark background fights the DaisyUI light theme <a class="header-anchor" href="#root-cause-2-—-storybook-decorator-s-dark-background-fights-the-daisyui-light-theme" aria-label="Permalink to &quot;Root cause #2 — Storybook decorator&#39;s dark background fights the DaisyUI light theme&quot;">​</a></h2><p><a href="./../../../../desktop/desktop-ui/.storybook/preview.tsx">desktop/desktop-ui/.storybook/preview.tsx</a> sets the default story background to <code>#0f172a</code> (dark navy):</p><div class="language-ts vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">backgrounds</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  default</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Soma surface&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  values</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    { name: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Soma surface&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, value: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;#0f172a&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    { name: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Paper&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, value: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;#f8fafc&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  ],</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">},</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br></div></div><p>But <a href="../../../../desktop/desktop-ui/src/styles.css">desktop/desktop-ui/src/styles.css</a> registers <code>cmyk --default, luxury</code> — so DaisyUI&#39;s default theme is light (<code>cmyk</code>). Components correctly use semantic colors (<code>text-base-content</code>, <code>bg-base-100</code>), which resolve to dark text on light backgrounds in <code>cmyk</code>. Render them on a hardcoded dark canvas and the text vanishes.</p><p>This is what creates the &quot;AI Input / AI Model Selector / PolymorphButton&quot; dark-on-dark reading. The components themselves aren&#39;t broken — the story chrome is fighting them.</p><p><strong>Fix options</strong> (Wave 0):</p><ol><li>Default the story background to <code>Paper</code> (the light value already in the array). Cheapest fix; honors the existing <code>cmyk</code> default.</li><li>Set <code>data-theme=&quot;luxury&quot;</code> on the story root when the background is dark, and <code>cmyk</code> when light. Story-by-story consistent, but every component must be readable in both — that&#39;s a separate audit.</li><li>Drop the dark background entirely and use the DaisyUI <code>base-100</code> color directly so the story background follows the theme.</li></ol><p>Recommendation: <strong>#3 — drop the hardcoded dark default</strong>, let <code>bg-base-100</code> (theme-driven) be the canvas. Each story can opt into the dark theme via <code>parameters.theme = &#39;luxury&#39;</code> for explicit dark-mode coverage.</p><h2 id="style-utilities-that-directly-contradict-adr-0005" tabindex="-1">Style utilities that directly contradict ADR-0005 <a class="header-anchor" href="#style-utilities-that-directly-contradict-adr-0005" aria-label="Permalink to &quot;Style utilities that directly contradict ADR-0005&quot;">​</a></h2><p><a href="../../../../desktop/desktop-ui/src/styles.css">styles.css</a> defines:</p><div class="language-css vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">css</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">@utility</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> glass-panel {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  @apply</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> border border-base-300/70 bg-base-100/70 backdrop-blur-xl shadow-2xl;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">@utility</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> surface-card {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  @apply</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> bg-base-100/80 border border-base-300/60 shadow-xl rounded-2xl;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br></div></div><p>Both use heavy shadows (<code>shadow-2xl</code>, <code>shadow-xl</code>) and oversized radius (<code>rounded-2xl</code>). ADR-0005 §7 locks &quot;1px border + bg-step on every elevated surface; <strong>shadow only on modal + popup window</strong>&quot; and §3 &quot;rounded-md (6px) max for surfaces&quot;. The overlay showcase&#39;s modal uses <code>glass-panel</code> and therefore inherits both bans-in-spirit.</p><p><strong>Fix</strong> (Wave 0 token sweep):</p><ul><li>Replace <code>surface-card</code> with a border-only variant (<code>border border-base-300/60 rounded-md bg-base-100</code>).</li><li>Replace <code>glass-panel</code> with a modal-and-popup-only <code>elevated-surface</code> utility that uses the single allowed shadow token + <code>rounded-md</code>.</li><li>Audit every consumer of these utilities (<code>grep -r glass-panel surface-card desktop/</code>) and migrate.</li></ul><h2 id="existing-components-that-do-work-—-keep-them" tabindex="-1">Existing components that <em>do</em> work — keep them <a class="header-anchor" href="#existing-components-that-do-work-—-keep-them" aria-label="Permalink to &quot;Existing components that *do* work — keep them&quot;">​</a></h2><p>These are the foundation we don&#39;t have to rebuild:</p><table tabindex="0"><thead><tr><th>Component</th><th>Path</th><th>Use in v0</th></tr></thead><tbody><tr><td><code>tapia/char-display.tsx</code></td><td><a href="./../../../../desktop/desktop-ui/src/components/tapia/char-display.tsx">link</a></td><td>Keep as is. Verify density tokens in Wave 1; otherwise no change.</td></tr><tr><td><code>chat/ai-conversation.tsx</code> + helpers</td><td><a href="./../../../../desktop/desktop-ui/src/components/chat/ai-conversation.tsx">link</a></td><td>Foundation for the right-area chat panel (PRD §4.5). Will need: composer-footer <code>BackendSwitcher</code> plug, mention-pill rendering for <code>@bot:</code>, streaming chrome alignment with <code>InlineAIStream</code>. Refactor, don&#39;t rebuild.</td></tr><tr><td><code>chat/ai-thinking.tsx</code></td><td><a href="./../../../../desktop/desktop-ui/src/components/chat/ai-thinking.tsx">link</a></td><td>The <code>Thinking…</code> pill we want both in chat (§4.5) and at the editor caret (§13 / <a href="./ui-revamp-v0-refs-editor-ai">refs editor-ai §5</a>). Already exists; extract into a shared primitive consumed by both.</td></tr><tr><td><code>layout/desktop-shell.tsx</code></td><td><a href="./../../../../desktop/desktop-ui/src/components/layout/desktop-shell.tsx">link</a></td><td>Closest precedent for the four-column shell. Needs additions: 52px icon spaces rail, right panel-stack column with the <code>PanelContainer</code>. Refactor target.</td></tr><tr><td><code>overlays/modal.tsx</code></td><td><a href="./../../../../desktop/desktop-ui/src/components/overlays/modal.tsx">link</a></td><td>Used by Overlays showcase; renders. Needs to swap off <code>glass-panel</code>&#39;s <code>shadow-2xl</code> once the token sweep lands.</td></tr><tr><td><code>overlays/toast.tsx</code></td><td><a href="./../../../../desktop/desktop-ui/src/components/overlays/toast.tsx">link</a></td><td>Already configured to use <code>bg-success</code> / <code>bg-error</code> semantic colors (see styles.css). Per ADR-0005 §6 toasts are restricted to the focus-task completion path; keep this one available, don&#39;t expand its uses.</td></tr></tbody></table><h2 id="updated-refactor-vs-rebuild-verdicts-overrides-scaffold-§2" tabindex="-1">Updated refactor-vs-rebuild verdicts (overrides scaffold §2) <a class="header-anchor" href="#updated-refactor-vs-rebuild-verdicts-overrides-scaffold-§2" aria-label="Permalink to &quot;Updated refactor-vs-rebuild verdicts (overrides scaffold §2)&quot;">​</a></h2><p>This audit overrides the verdicts in <a href="./ui-revamp-v0-scaffold#2-mapping-prd-6-to-current-soma-ui-reality">ui-revamp-v0-scaffold.md §2</a>:</p><table tabindex="0"><thead><tr><th>PRD §6 component</th><th>Original verdict</th><th>Audit-corrected verdict</th></tr></thead><tbody><tr><td><code>BackendSwitcher</code></td><td>Refactor <code>forms/ai-model-selector.tsx</code></td><td><strong>Rebuild.</strong> Existing component renders broken even in its sole story (theme + clipped dropdown). Move logic to a new <code>chat/backend-switcher.tsx</code> patterned on Rox 438aaaec from <a href="./ui-revamp-v0-refs">refs main §5</a>; discard the old file.</td></tr><tr><td><code>SelectionBubble</code></td><td>Refactor <code>overlays/bubble-toolbar.tsx</code> (65 lines)</td><td><strong>Rebuild.</strong> No story, no visual confidence. Read the existing file for prior intent, then write fresh against <a href="./ui-revamp-v0-refs-editor">refs editor §2</a>.</td></tr><tr><td><code>CommandPalette</code></td><td>Refactor <code>overlays/command-palette.tsx</code> (166 lines)</td><td><strong>Story-first refactor.</strong> Add a story exercising the locked sections (Recent docs → Spaces → Documents → Commands) <em>before</em> re-sectioning. If the existing internals don&#39;t survive contact with the story, rewrite.</td></tr><tr><td><code>DenseRow</code></td><td>Refactor <code>lists/roster-item.tsx</code></td><td><strong>Story-first refactor.</strong> Same posture — no story today, low confidence in the existing slot model. Story-first, then keep what survives.</td></tr><tr><td><code>PopupShell</code></td><td>Refactor <code>layout/window-chrome.tsx</code></td><td><strong>Story-first refactor.</strong> Same posture.</td></tr><tr><td><code>PolymorphButton</code> (existing — not in PRD §6)</td><td>n/a</td><td><strong>Fix the default.</strong> Flip <code>shape = &quot;default&quot;</code>. Cross-cutting bug; Wave 1 scope.</td></tr></tbody></table><p>Components verdict unchanged from scaffold §2: all new components, <code>Panel</code> / <code>PanelContainer</code> / <code>Empty</code> / <code>SettingsTabs</code> / <code>Pill</code> / <code>MentionPicker</code> / <code>SlashMenu</code> / <code>BotList</code> / <code>CapabilityForm</code> / <code>PeerAddressInput</code> / <code>TreePopover</code> / <code>NodeAIRegistry</code> / <code>SelectionAIBar</code> / <code>InlineAIAcceptBar</code> / <code>InlineAIStream</code> / <code>SpacesRail</code> / <code>DensityProvider</code>.</p><h2 id="wave-0-additions" tabindex="-1">Wave 0 additions <a class="header-anchor" href="#wave-0-additions" aria-label="Permalink to &quot;Wave 0 additions&quot;">​</a></h2><p>The token sweep already specified in the scaffold gains two concrete tasks:</p><ol><li><strong>Storybook decorator fix.</strong> Drop the hardcoded <code>#0f172a</code> default background from <a href="./../../../../desktop/desktop-ui/.storybook/preview.tsx">.storybook/preview.tsx</a>; let <code>bg-base-100</code> carry the canvas. Add a per-story <code>parameters.theme</code> knob for explicit <code>cmyk</code> / <code>luxury</code> testing.</li><li><strong>Story-first acceptance.</strong> No new story written in Wave 1 may render unreadably on the default background. CI check: a small Playwright story-render test that asserts text contrast ≥ AA against the surface background. (Cheap; the existing Storybook test-runner is sufficient.)</li></ol><p>Plus the original sweep tasks:</p><ul><li>Add density tokens (font sizes, line-heights, row heights) to <a href="../../../../desktop/desktop-ui/src/styles.css">styles.css</a>.</li><li>Kill <code>glass-panel</code> (it inlines <code>shadow-2xl</code>) and <code>surface-card</code> (it inlines <code>shadow-xl rounded-2xl</code>); replace with token-driven versions.</li><li>Audit + migrate consumers of those utilities.</li></ul><h2 id="what-this-means-for-the-scaffold-s-cutover-order" tabindex="-1">What this means for the scaffold&#39;s cutover order <a class="header-anchor" href="#what-this-means-for-the-scaffold-s-cutover-order" aria-label="Permalink to &quot;What this means for the scaffold&#39;s cutover order&quot;">​</a></h2><p>Unchanged. The first end-user-visible cutover is still space settings + Bots tab — the components feeding it (<code>SettingsTabs</code>, <code>BotList</code>, <code>PeerAddressInput</code>, <code>CapabilityForm</code>, <code>Empty</code>, <code>DenseRow</code>, <code>Pill</code>, <code>DensityProvider</code>) are all <strong>new</strong> or <strong>story-first refactor</strong>, so the audit doesn&#39;t change the dependency graph.</p><p>What <em>does</em> change: the chat panel cutover (cutover 3 in scaffold §5) now leans on <code>chat/ai-conversation.tsx</code> as a working foundation rather than a refactor risk. That cutover gets cheaper than the scaffold suggested.</p><h2 id="open-follow-up" tabindex="-1">Open follow-up <a class="header-anchor" href="#open-follow-up" aria-label="Permalink to &quot;Open follow-up&quot;">​</a></h2><ul><li><strong>Theme matrix.</strong> Once the storybook decorator is fixed, run every story under both <code>cmyk</code> and <code>luxury</code> and capture any components that fail contrast in either. Likely 3–5 components; surface them as separate Wave 1 issues.</li><li><strong>Story coverage debt.</strong> Every claimed-refactor component (command-palette, bubble-toolbar, context-menu, etc.) needs a story before the refactor begins. This is mechanical work and a good first-task for an implementer onboarding to the codebase.</li><li><strong><code>PolymorphButton</code> consumer sweep.</strong> After flipping the default to <code>default</code>, grep every caller; many likely currently pass <code>shape=&quot;default&quot;</code> explicitly. Drop those — they&#39;re the workaround for the bad default.</li><li><strong>Yoopta subpath.</strong> <code>AGENTS.md</code> (line 167, at the repo root — outside this docs tree) lists <code>@soma/ui/yoopta</code> as a subpath. The PRD treats the editor as TipTap-only. Confirm whether <code>@soma/ui/yoopta</code> still ships anything or is dead code to delete in the cleanup.</li></ul><hr><p><em>This audit is dated; rerun when material UI work lands. The Storybook test-runner contrast check (Wave 0 §2 above) is what keeps it dated automatically.</em></p>`,44)])])}const k=t(d,[["render",r]]);export{u as __pageData,k as default};
