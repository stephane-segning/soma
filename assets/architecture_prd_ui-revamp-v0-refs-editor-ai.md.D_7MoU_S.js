import{_ as t,o,c as a,a2 as n}from"./chunks/framework.BDCokyix.js";const u=JSON.parse('{"title":"UI Revamp v0 — Editor AI Inspiration References","description":"","frontmatter":{},"headers":[],"relativePath":"architecture/prd/ui-revamp-v0-refs-editor-ai.md","filePath":"architecture/prd/ui-revamp-v0-refs-editor-ai.md","lastUpdated":1780254295000}'),r={name:"architecture/prd/ui-revamp-v0-refs-editor-ai.md"};function i(s,e,d,l,c,h){return o(),a("div",null,[...e[0]||(e[0]=[n('<h1 id="ui-revamp-v0-—-editor-ai-inspiration-references" tabindex="-1">UI Revamp v0 — Editor AI Inspiration References <a class="header-anchor" href="#ui-revamp-v0-—-editor-ai-inspiration-references" aria-label="Permalink to &quot;UI Revamp v0 — Editor AI Inspiration References&quot;">​</a></h1><p>Companion to <a href="./ui-revamp-v0">ui-revamp-v0.md</a>, the existing inspiration overview <a href="./ui-revamp-v0-refs">ui-revamp-v0-refs.md</a>, and especially <a href="./../adrs/0005-ui-revamp-v0">ADR-0005 §11</a>. Findings from a focused Refero pass on <strong>in-editor AI as a content-transformation surface</strong>.</p><p>This is a <strong>new, third editor surface</strong> — not a contradiction of locked decisions. ADR-0005 §11 explicitly bars an AI tile from the SlashMenu (which inserts blocks) and PRD §4.5 anchors the LLM conversation in the right-area chat panel. Neither covers the case where the user wants to <em>act on existing content</em> — rewrite a selection, continue from the caret, summarize three blocks, caption an image. That is a transformation, not a conversation, and not a block insert. The pattern this doc lays out is what TipTap/Notion/Hashnode call <strong>inline AI</strong> (also &quot;AI command&quot; or &quot;Ask AI&quot;): a transient menu bound to a selection / cursor / node, with results that land directly in the document.</p><h2 id="_1-selection-based-ai-action-prd-§4-5-adr-0005-§11" tabindex="-1">1. Selection-based AI action (PRD §4.5, ADR-0005 §11) <a class="header-anchor" href="#_1-selection-based-ai-action-prd-§4-5-adr-0005-§11" aria-label="Permalink to &quot;1. Selection-based AI action (PRD §4.5, ADR-0005 §11)&quot;">​</a></h2><p>User highlights a span of text → invokes an AI action → result replaces or augments the selection.</p><h3 id="borrow" tabindex="-1">Borrow <a class="header-anchor" href="#borrow" aria-label="Permalink to &quot;Borrow&quot;">​</a></h3><ul><li><strong>Grammarly &quot;GrammarlyGO&quot; rewrite panel</strong> (<a href="https://refero.design/pages/ec09dfe8-a65a-4368-98cd-6bc034fb835d" target="_blank" rel="noreferrer">screen ec09dfe8</a> and Flow <a href="https://refero.design/flows/1875" target="_blank" rel="noreferrer">1875</a>). User highlights a paragraph; a popover anchored just above the selection appears with the rewrite preview and a row of <strong>tone chips</strong> (&quot;Shorten it&quot;, &quot;Make it assertive&quot;, &quot;Sound confident&quot;) under the primary <code>Insert</code> / <code>Rephrase</code> actions. The selection stays highlighted <em>behind</em> the popover the whole time — the user can see what is being acted on. This is the strongest concrete pattern for our selection-based action and matches our density goals.</li><li><strong>Hashnode &quot;Choose a prompt&quot; dropdown</strong> (<a href="https://refero.design/pages/26b8c405-ae00-4f40-96c2-5c0a6dfb05b0" target="_blank" rel="noreferrer">screen 26b8c405</a>, Flow <a href="https://refero.design/flows/3921" target="_blank" rel="noreferrer">3921</a>). A single text input pinned at the top of the editor — <code>Choose a prompt or type a new one</code> — opens a categorized menu: <strong>REWRITE</strong> (Improve writing, Fix spelling and grammar, Rewrite as a single sentence, Simplify language), <strong>MODIFY</strong> (Shorten / Expand / Summarize / Explain), <strong>TONE</strong>, <strong>TRANSFORM</strong> (Turn into bullet list, numbered list). This sectioning is exactly the IA we need: it gives users keyboard-first access to ~5 canned actions per group, but the field is also a free-text prompt, which doubles as the &quot;Custom prompt&quot; affordance.</li><li><strong>Cycle inline prompt bar</strong> (<a href="https://refero.design/pages/40c12209-e599-4220-a8a8-16f4e8168a88" target="_blank" rel="noreferrer">screen 40c12209</a>). Shows the same pattern in a denser, action-bar form factor: <code>Continue writing · Shorten · Simplify · Spelling &amp; Grammar · Emojify · Translate · Tone of Voice · Summarize</code>, all as 11-12px chips in a single floating bar. This is closer to our density target than Grammarly&#39;s larger card and is the right visual reference for the dense Soma version.</li></ul><h3 id="synthesis-selectionaibar-component" tabindex="-1">Synthesis: SelectionAIBar component <a class="header-anchor" href="#synthesis-selectionaibar-component" aria-label="Permalink to &quot;Synthesis: SelectionAIBar component&quot;">​</a></h3><p>Trigger: text selected → either (a) <code>⌘J</code> (or <code>⌘.</code>), (b) a small &quot;Ask AI&quot; chip appended to the right end of the existing <a href="./../adrs/0005-ui-revamp-v0#11-editor-surfaces-slash-menu-selection-bubble-block-menus"><code>SelectionBubble</code></a>, or (c) typing <code>/</code> <em>while a selection is non-empty</em> (existing slash menu transforms into an AI-action-only variant — same caret target, different intent). The bubble does <strong>not absorb</strong> these actions — it stays a formatting surface. The AI chip is the bridge between the two.</p><p>Surface: single rounded popover above the selection (selection stays visible behind it). Top row = text input with placeholder <code>Ask AI to edit or transform…</code>. Body = sectioned action list (Rewrite / Modify / Tone / Transform / Translate) keyboard-traversable. Bottom row = recently-used actions. ~14px input, 13px rows.</p><h3 id="avoid" tabindex="-1">Avoid <a class="header-anchor" href="#avoid" aria-label="Permalink to &quot;Avoid&quot;">​</a></h3><ul><li><strong>Grammarly&#39;s right-side AI assistant card</strong> (<a href="https://refero.design/pages/caceccf5-d1b4-4bed-aae6-4f1ef3e15fb5" target="_blank" rel="noreferrer">screen caceccf5</a>). Pushing the suggestion into a permanent right-side panel collides with Soma&#39;s existing chat panel and re-introduces the &quot;everything goes in a side sheet&quot; trap. The Grammarly popover <em>above the selection</em> is the right move; their full side panel is not.</li><li>Floating round &quot;AI&quot; buttons that hover next to every paragraph (Notion&#39;s hover-rail AI). Too noisy on 14&quot; laptops; we already use the left gutter for drag + insert, and adding a third icon there is over-budget.</li><li>ChatGPT-style &quot;send to chat to rewrite this&quot; flows. Conversation is the wrong shape for &quot;make this one paragraph shorter.&quot;</li></ul><h2 id="_2-cursor-position-ai-prd-§6-slashmenu-adr-0005-§11" tabindex="-1">2. Cursor-position AI (PRD §6 SlashMenu, ADR-0005 §11) <a class="header-anchor" href="#_2-cursor-position-ai-prd-§6-slashmenu-adr-0005-§11" aria-label="Permalink to &quot;2. Cursor-position AI (PRD §6 SlashMenu, ADR-0005 §11)&quot;">​</a></h2><p>Empty caret → user invokes → AI writes new content at the caret.</p><h3 id="borrow-1" tabindex="-1">Borrow <a class="header-anchor" href="#borrow-1" aria-label="Permalink to &quot;Borrow&quot;">​</a></h3><ul><li><strong>Hashnode prompt input on empty caret</strong> (same Flow <a href="https://refero.design/flows/3921" target="_blank" rel="noreferrer">3921</a>). When no selection exists, the same <code>Choose a prompt or type a new one</code> field becomes a <em>generation</em> prompt rather than a <em>transformation</em> one. Same menu, different default action set: REWRITE is hidden, <strong>Continue writing</strong>, <strong>Brainstorm ideas</strong>, <strong>Outline a section</strong>, <strong>Summarize document</strong> become the primary entries. This proves the same surface can serve both selection and caret with conditional content.</li><li><strong>Slite editor <code>/</code> AI prompt</strong> (<a href="https://refero.design/pages/345c7168-a3ca-415b-9596-ebef6e6ce837" target="_blank" rel="noreferrer">screen 345c7168</a>). Slite uses an &quot;ask AI&quot; prompt at the top of the slash menu <em>as a search field</em>, not a tile. Typing <code>/</code> opens block options; typing <code>/ &lt;free text&gt;</code> and pressing Enter dispatches the free text to AI. This sidesteps the ADR-0005 §11 ban (no AI <strong>tile</strong>) while still letting <code>/</code> be the invocation key. We can adopt this with a clear visual divider: the field on top is <em>AI prompt</em>, the rows below are <em>block inserts</em>, and the two never collapse into one list.</li></ul><h3 id="synthesis-how-this-slots-into-the-locked-slashmenu" tabindex="-1">Synthesis: how this slots into the locked SlashMenu <a class="header-anchor" href="#synthesis-how-this-slots-into-the-locked-slashmenu" aria-label="Permalink to &quot;Synthesis: how this slots into the locked SlashMenu&quot;">​</a></h3><p>ADR-0005 §11 says the slash menu has no AI tile. We hold that lock. <strong>What we add</strong> is a header row above the existing Text/List/Embed/Action/Advanced sections: a single input row that, when the user types anything other than a block name, transforms the popover into an inline-AI prompt (loses the section list, shows recent prompts + &quot;Continue writing&quot; / &quot;Brainstorm&quot; / &quot;Custom prompt&quot;). If the user presses <code>↓</code> or types a known block label, it&#39;s back to block-insert mode. This is <strong>one surface, two modes</strong> — not a duplicate AI tile.</p><p>For users who don&#39;t want to lean on <code>/</code>, the same surface is reachable via <code>⌘J</code>. Keyboard-first; no floating button cluttering the gutter.</p><h3 id="avoid-1" tabindex="-1">Avoid <a class="header-anchor" href="#avoid-1" aria-label="Permalink to &quot;Avoid&quot;">​</a></h3><ul><li>A second slash command like <code>/ai</code> or <code>++</code> that opens a different popover. ADR-0005 §11 explicitly warns against duplicate AI invocation paths; we keep one.</li><li>Auto-suggested ghost text on every empty paragraph (Notion&#39;s old behavior, since toned down). Too noisy, and impossible to reconcile with offline-first / local-first model loading delays we expect.</li></ul><h2 id="_3-block-level-ai-on-non-text-nodes-new-—-extends-prd-§4-9-§6" tabindex="-1">3. Block-level AI on non-text nodes (new — extends PRD §4.9, §6) <a class="header-anchor" href="#_3-block-level-ai-on-non-text-nodes-new-—-extends-prd-§4-9-§6" aria-label="Permalink to &quot;3. Block-level AI on non-text nodes (new — extends PRD §4.9, §6)&quot;">​</a></h2><p>Right-click or hover on an image / table / code block / embed → AI action available on that specific node.</p><h3 id="borrow-2" tabindex="-1">Borrow <a class="header-anchor" href="#borrow-2" aria-label="Permalink to &quot;Borrow&quot;">​</a></h3><ul><li><strong>Medium &quot;Alternative text&quot; modal</strong> (<a href="https://refero.design/pages/9763cb47-b166-4e1c-87f6-e5c7938bf54b" target="_blank" rel="noreferrer">screen 9763cb47</a>). Centered modal over a dimmed page: title <code>Alternative text</code>, single-line description <code>Write a brief description of this image for readers with visual impairments</code>, the image preview, a single input pre-filled with a generated placeholder (<code>E.g., An antique typewriter with a blank sheet of paper sits on a wooden desk</code>), and <code>Save</code> / <code>Cancel</code>. We borrow the structure but flip the workflow: rather than the user writing alt and AI being a hint, we open the modal <strong>with the AI-generated alt already filled in</strong>, and the user accepts or edits. This is one click cheaper.</li><li><strong>Raster auto-generated image descriptions blog</strong> (<a href="https://refero.design/pages/7f4af06b-7539-400d-b63c-8d1bac6907eb" target="_blank" rel="noreferrer">screen 7f4af06b</a>). Confirms the product framing: per-image AI generates a description on demand, stored on the asset. We get the framing and copy patterns from this, not the UI.</li><li><strong>Per-node AI menu pattern (cross-product convention).</strong> Right-click on an image in Notion / Coda exposes a node-typed submenu: for images, &quot;Generate alt text&quot;, &quot;Describe&quot;, &quot;Caption&quot;; for tables, &quot;Analyze&quot;, &quot;Summarize columns&quot;, &quot;Find outliers&quot;; for code blocks, &quot;Explain&quot;, &quot;Refactor&quot;, &quot;Add comments&quot;; for embed/sub-page nodes, &quot;Summarize linked page&quot;. The right-click menu (already locked as the per-block menu in ADR-0005 §11) is the natural home — we add an <code>AI ▸</code> section above the standard <code>Duplicate</code> / <code>Move to</code> / <code>Delete</code> cluster, with node-type-specific entries.</li></ul><h3 id="synthesis-node-typed-ai-sections" tabindex="-1">Synthesis: node-typed AI sections <a class="header-anchor" href="#synthesis-node-typed-ai-sections" aria-label="Permalink to &quot;Synthesis: node-typed AI sections&quot;">​</a></h3><p>Each node type registers its own AI submenu. The slash AI prompt and the SelectionAIBar reuse the same action registry — image nodes contribute <code>generate-alt-text</code>, <code>caption</code>, <code>describe-for-search</code>; code nodes contribute <code>explain</code>, <code>refactor</code>, <code>find-bug</code>; etc. This is what makes &quot;block-level AI&quot; not a one-off feature but a system: every block knows what it lets the AI do to it. Component-wise, this becomes a <code>NodeAIRegistry</code> on the <code>@soma/ui</code> editor primitives.</p><h3 id="avoid-2" tabindex="-1">Avoid <a class="header-anchor" href="#avoid-2" aria-label="Permalink to &quot;Avoid&quot;">​</a></h3><ul><li>A separate right-area panel for image alt-text. Overkill — alt text is a one-line field on the node, not a conversation.</li><li>Hovering image → showing an inline &quot;AI&quot; floating button on the corner of the image. Visual noise; the right-click menu already exists and is locked.</li></ul><h2 id="_4-accept-reject-regenerate-ux-new-—-anchored-against-adr-0005-§11" tabindex="-1">4. Accept / reject / regenerate UX (new — anchored against ADR-0005 §11) <a class="header-anchor" href="#_4-accept-reject-regenerate-ux-new-—-anchored-against-adr-0005-§11" aria-label="Permalink to &quot;4. Accept / reject / regenerate UX (new — anchored against ADR-0005 §11)&quot;">​</a></h2><p>After AI writes into the document, the user needs to accept, reject, refine, or try again.</p><h3 id="borrow-3" tabindex="-1">Borrow <a class="header-anchor" href="#borrow-3" aria-label="Permalink to &quot;Borrow&quot;">​</a></h3><ul><li><strong>Hashnode &quot;Improve writing&quot; preview modal</strong> (<a href="https://refero.design/pages/988c1f37-82f5-4ebb-a222-22d5527b29bc" target="_blank" rel="noreferrer">screen 988c1f37</a>, Flow <a href="https://refero.design/flows/3921" target="_blank" rel="noreferrer">3921</a> step 6). The clearest reference. After the user picks an action, a small modal centered over the document shows: <ul><li>Header: action name (<code>Improve writing</code>) + close <code>×</code></li><li>Body: the generated paragraph in a single readable block (no diff yet — just the result), with the original selection still highlighted <em>behind</em> the modal</li><li>Footer: <code>Regenerate</code> (secondary, grey) on the left, <code>Accept</code> (primary, blue) on the right This is a great floor because it&#39;s tiny, decisive, and never blocks the user from reading the original (the modal is small enough that the highlighted span shows underneath). The &quot;no diff yet&quot; choice is the right one for v0 — diffs come later (see open follow-up).</li></ul></li><li><strong>Grammarly multi-variant card</strong> (<a href="https://refero.design/pages/2d41f4dc-ade4-4583-8f4b-963a75aab3a8" target="_blank" rel="noreferrer">screen 2d41f4dc</a>). Shows two-three alternate rewrites in a stack with <code>Insert</code> on each, plus a <code>Tell us to…</code> refinement field at the bottom of the card. This is the pattern we want for the <strong>&quot;refine&quot; affordance</strong>: instead of regenerating blindly, the user can type &quot;make it shorter&quot; and the same card updates. We borrow that footer-input pattern.</li></ul><h3 id="synthesis-in-place-result-with-action-bar" tabindex="-1">Synthesis: in-place result with action bar <a class="header-anchor" href="#synthesis-in-place-result-with-action-bar" aria-label="Permalink to &quot;Synthesis: in-place result with action bar&quot;">​</a></h3><p>When the user invokes inline AI:</p><ol><li><strong>Pending state.</strong> The selection (or insertion zone) gets a tinted background (<code>base-300</code> step + 1px dashed <code>accent</code> border — borders not shadows per ADR-0005 §7) and the new text streams in. Original text stays underneath, semi-transparent at 40% opacity, so the user can read both.</li><li><strong>Result state.</strong> Once streaming finishes, a single dense action bar appears anchored to the bottom of the inserted region: <code>✓ Accept</code> · <code>↻ Try again</code> · <code>✎ Refine…</code> · <code>✕ Discard</code>. <code>Refine…</code> reopens the prompt input pre-filled with the previous prompt for one-tap edit. <code>Try again</code> re-runs the same prompt.</li><li><strong>Accept.</strong> Original is dropped, AI text becomes plain document text, the bar closes.</li><li><strong>Discard.</strong> AI text is removed, original returns to full opacity.</li></ol><p>This avoids the multi-modal stack Hashnode uses (modal on top of editor) and stays in the document, which matches Soma&#39;s density rules.</p><h3 id="avoid-3" tabindex="-1">Avoid <a class="header-anchor" href="#avoid-3" aria-label="Permalink to &quot;Avoid&quot;">​</a></h3><ul><li>A diff view with red/green strikethrough as the default (Cursor / Copilot Chat). Powerful for code, but visual overload for prose — and our editor is prose-first. Move it to an opt-in &quot;diff mode&quot; post-v0.</li><li>Toast-only feedback (&quot;AI suggestion ready, click here to view&quot;). Banned by ADR-0005 §6 for primary actions; inline AI is a primary action.</li><li>A separate &quot;history of AI rewrites&quot; sub-panel. Overkill for v0; the chat panel already keeps a transcript if the user routed through it.</li></ul><h2 id="_5-streaming-into-document-ux-new-—-extends-prd-§4-5-streaming" tabindex="-1">5. Streaming-into-document UX (new — extends PRD §4.5 streaming) <a class="header-anchor" href="#_5-streaming-into-document-ux-new-—-extends-prd-§4-5-streaming" aria-label="Permalink to &quot;5. Streaming-into-document UX (new — extends PRD §4.5 streaming)&quot;">​</a></h2><p>The chat panel already handles streaming in conversations (PRD §4.5: <code>Thinking…</code> pill, partial tokens, stop button). The in-editor case is different because tokens land <em>in the document itself</em>.</p><h3 id="borrow-4" tabindex="-1">Borrow <a class="header-anchor" href="#borrow-4" aria-label="Permalink to &quot;Borrow&quot;">​</a></h3><ul><li><strong>Grammarly &quot;Working on it…&quot; state</strong> (Flow <a href="https://refero.design/flows/1875" target="_blank" rel="noreferrer">1875</a> step 3). While generating, the card shows a skeleton with a pulse animation; the existing selection stays highlighted; the rest of the document stays interactive (the user can scroll, just not edit the in-flight region). This is the right baseline: lock the inserted region, leave everything else alone.</li><li><strong>Hashnode &quot;Continue writing&quot;</strong> (in <a href="https://refero.design/pages/40c12209-e599-4220-a8a8-16f4e8168a88" target="_blank" rel="noreferrer">40c12209</a>). Streaming text appears one paragraph at a time, with the cursor at the trailing edge of the streamed content; a small <code>Stop</code> chip appears just below the in-flight region.</li><li><strong>Reuse the chat panel&#39;s <code>Thinking…</code> pill</strong> (locked in PRD §4.5) for the pre-first-token state at the caret. Same component, anchored to caret instead of chat.</li></ul><h3 id="synthesis-streaming-chrome-at-the-caret" tabindex="-1">Synthesis: streaming chrome at the caret <a class="header-anchor" href="#synthesis-streaming-chrome-at-the-caret" aria-label="Permalink to &quot;Synthesis: streaming chrome at the caret&quot;">​</a></h3><ul><li><strong>In-flight tint.</strong> Streamed tokens render with the <code>accent</code> color text and a 1px dashed <code>accent</code> underline (no full background tint — we want the user to read the prose). The region is <code>contenteditable=false</code> until streaming completes.</li><li><strong>Pre-first-token.</strong> Replace caret blink with the <code>Thinking…</code> pill (same chip as chat panel) inline at the insertion point. Width animates from 0 → fit-content. No spinner — the pulse on the pill is sufficient.</li><li><strong>Stop control.</strong> A solid round stop button sits at the end of the in-flight region (mirrors the chat panel&#39;s composer stop, ADR-0005 / PRD §4.5). One control, one place to look.</li><li><strong>Post-streaming.</strong> Underline fades over 200ms; text snaps to normal color; the accept bar appears (see §4 above).</li></ul><h3 id="avoid-4" tabindex="-1">Avoid <a class="header-anchor" href="#avoid-4" aria-label="Permalink to &quot;Avoid&quot;">​</a></h3><ul><li>Solid background tint for streamed text (reduces readability while it&#39;s streaming).</li><li>A spinner overlaying the editor. The <code>Thinking…</code> pill is enough.</li><li>Letting the user keep typing in the in-flight region. Race conditions and lost edits. Lock it.</li></ul><h2 id="_6-relationship-to-the-chat-panel-prd-§4-5" tabindex="-1">6. Relationship to the chat panel (PRD §4.5) <a class="header-anchor" href="#_6-relationship-to-the-chat-panel-prd-§4-5" aria-label="Permalink to &quot;6. Relationship to the chat panel (PRD §4.5)&quot;">​</a></h2><p>This is the design call the lock pass most needs to make. The options:</p><table tabindex="0"><thead><tr><th>Option</th><th>What it means</th><th>Verdict</th></tr></thead><tbody><tr><td><strong>A. Inline AI replaces chat for transformations</strong></td><td>All AI on existing content goes through SelectionAIBar / caret prompt. Chat is for free-form Q&amp;A only.</td><td>Too clean; loses the &quot;summarize this doc into a chat thread&quot; use case.</td></tr><tr><td><strong>B. Inline AI complements chat — full silo</strong></td><td>Inline AI never talks to chat; chat never sees inline rewrites. Two tracks.</td><td>Simple but throws away history — users can&#39;t go back and see what they rewrote three days ago.</td></tr><tr><td><strong>C. Inline AI is a pre-fill bridge into chat</strong></td><td>Selecting text + invoking AI opens a transient prompt above the selection, but every action is <em>also</em> recorded as a turn in the right-area chat panel. The chat panel is the audit log; the inline surface is the input + output.</td><td><strong>Recommended.</strong></td></tr></tbody></table><h3 id="recommendation-option-c-—-bridge-with-optional-escalation" tabindex="-1">Recommendation: Option C — bridge with optional escalation <a class="header-anchor" href="#recommendation-option-c-—-bridge-with-optional-escalation" aria-label="Permalink to &quot;Recommendation: Option C — bridge with optional escalation&quot;">​</a></h3><p>Default flow:</p><ol><li>User selects text, presses <code>⌘J</code>. Inline prompt appears above selection.</li><li>User picks &quot;Make shorter&quot; (or types a custom prompt).</li><li>AI streams into the document; inline accept bar appears.</li><li><strong>In parallel</strong>, a collapsed chat turn is appended to the chat panel: <code>&lt;doc-ref pill&gt; → Make shorter</code> with the result. The user doesn&#39;t see this unless they look — it&#39;s collapsed by default.</li><li>If the user wants to iterate further (&quot;now make it 3 bullets&quot;), they can either: <ul><li>Use the inline <code>Refine…</code> action (cheaper, stays in document), or</li><li>Click &quot;Open in chat&quot; on the inline accept bar (expanded interaction, full conversation with the rewrite as context).</li></ul></li></ol><p>This gives us:</p><ul><li><strong>Inline AI for the 80% case</strong> (fast, in-document, no panel context switch).</li><li><strong>Chat panel for the 20%</strong> when the rewrite becomes a conversation (compare drafts, branch, dig deeper).</li><li><strong>One audit log</strong> for both, anchored in the locked chat panel.</li></ul><p>The chat panel does not need a new mode for this. The new chat-message type is just <code>inline-edit</code>, rendered as a small folded pill instead of a normal message bubble.</p><h3 id="avoid-5" tabindex="-1">Avoid <a class="header-anchor" href="#avoid-5" aria-label="Permalink to &quot;Avoid&quot;">​</a></h3><ul><li>Always opening the chat panel for any inline AI invocation. Defeats the speed gain.</li><li>Never recording inline AI activity anywhere. Users lose the ability to reconstruct what they edited.</li></ul><h2 id="_7-pattern-locks-recommendations-for-adr-0005-addendum" tabindex="-1">7. Pattern locks (recommendations for ADR-0005 addendum) <a class="header-anchor" href="#_7-pattern-locks-recommendations-for-adr-0005-addendum" aria-label="Permalink to &quot;7. Pattern locks (recommendations for ADR-0005 addendum)&quot;">​</a></h2><table tabindex="0"><thead><tr><th>PRD ref</th><th>Pattern locked from refs</th><th>Reference</th></tr></thead><tbody><tr><td>§6 new component</td><td><code>SelectionAIBar</code> — popover above selection, top input + categorized action list (Rewrite / Modify / Tone / Transform / Translate / Custom prompt). Density: 14px input, 13px rows.</td><td>Grammarly ec09dfe8, Hashnode 26b8c405, Cycle 40c12209</td></tr><tr><td>§6 new component / §11 amendment</td><td>Inline AI invoked via <code>⌘J</code>, the &quot;Ask AI&quot; chip on the SelectionBubble, OR <code>/</code> + free text (SlashMenu&#39;s existing input row, gated on no block-name match)</td><td>Slite 345c7168, Hashnode 26b8c405</td></tr><tr><td>§6 new behavior</td><td>Caret AI uses the same SlashMenu popover, but with a <em>generation</em> action set (Continue writing / Brainstorm / Outline / Summarize document). No new tile; conditional content.</td><td>Hashnode flow 3921 step 2</td></tr><tr><td>§6 new system</td><td><code>NodeAIRegistry</code> — every block type registers AI actions, exposed in the right-click block menu under an <code>AI ▸</code> cluster (above the existing Duplicate / Move / Delete cluster)</td><td>Medium 9763cb47, Raster 7f4af06b</td></tr><tr><td>§6 new behavior</td><td>Accept / Try-again / Refine / Discard bar anchored under inserted region. No diff view in v0. Refine reopens the prompt input pre-filled.</td><td>Hashnode 988c1f37, Grammarly 2d41f4dc</td></tr><tr><td>§4.5 extension</td><td>Streamed tokens get accent-colored text + dashed accent underline. Region is locked while streaming. Stop chip at trailing edge. <code>Thinking…</code> pill (reused from chat) for pre-first-token.</td><td>Grammarly flow 1875 step 3</td></tr><tr><td>§4.5 extension</td><td>Every inline AI invocation appends a collapsed turn to the chat panel as audit log. Inline accept bar exposes <code>Open in chat</code> for escalation.</td><td>(synthesis; no single screen)</td></tr></tbody></table><h2 id="_8-adr-0005-addendum-proposal" tabindex="-1">8. ADR-0005 addendum proposal <a class="header-anchor" href="#_8-adr-0005-addendum-proposal" aria-label="Permalink to &quot;8. ADR-0005 addendum proposal&quot;">​</a></h2><p><strong>Recommendation: amend ADR-0005 with a new §13 decision, and reference it from §11.</strong> A single new component slot in PRD §6 is <em>not</em> sufficient — inline AI changes how three existing surfaces (SlashMenu, SelectionBubble, block menus) behave, plus introduces a streaming-into-document state and a cross-surface audit-log convention. That is the threshold for an ADR-level decision, not just a component.</p><h3 id="proposed-wording" tabindex="-1">Proposed wording <a class="header-anchor" href="#proposed-wording" aria-label="Permalink to &quot;Proposed wording&quot;">​</a></h3><p><strong>Amend §11</strong> to add a final paragraph:</p><blockquote><p>Inline AI is a third editor surface alongside SlashMenu and SelectionBubble — see §13. SlashMenu still has no AI tile (ADR lock); instead, the SlashMenu&#39;s leading input row doubles as the AI prompt entry when the typed text does not match a block name. SelectionBubble grows a trailing &quot;Ask AI&quot; chip that opens the SelectionAIBar; the bubble itself remains a formatting surface. Block menus (right-click) gain an <code>AI ▸</code> cluster fed by the per-node-type AI registry.</p></blockquote><p><strong>Add §13 &quot;Inline AI as a content-transformation surface&quot;</strong>:</p><blockquote><p>Inline AI is a transformation surface bound to a <em>selection</em>, <em>caret position</em>, or <em>node</em> — distinct from the conversational chat panel (§4.5) and from the block-inserting SlashMenu (§11). It is invoked by <code>⌘J</code>, by the trailing chip on the SelectionBubble, by the SlashMenu input row when not matching a block name, or by the <code>AI ▸</code> cluster in a node&#39;s right-click menu. Results stream into the document at the invocation site; a single accept bar (<code>Accept</code> · <code>Try again</code> · <code>Refine…</code> · <code>Discard</code>) closes the interaction. While streaming, the affected region is non-editable and renders in the accent color with a dashed accent underline; a <code>Thinking…</code> pill (same component as the chat panel, §4.5) covers the pre-first-token window. Every inline AI invocation appends a collapsed turn to the chat panel as audit log, and the inline accept bar exposes &quot;Open in chat&quot; for escalation into a full conversation. No diff view, no per-paragraph hover AI button, no separate <code>/ai</code> slash command — one surface, three invocation paths, one accept bar.</p></blockquote><h3 id="proposed-prd-§6-component-additions" tabindex="-1">Proposed PRD §6 component additions <a class="header-anchor" href="#proposed-prd-§6-component-additions" aria-label="Permalink to &quot;Proposed PRD §6 component additions&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Component</th><th>Purpose</th></tr></thead><tbody><tr><td><code>SelectionAIBar</code></td><td>Inline AI popover above selection — prompt input + categorized action list</td></tr><tr><td><code>InlineAIAcceptBar</code></td><td>Accept / Try-again / Refine / Discard anchored under the inserted region</td></tr><tr><td><code>NodeAIRegistry</code></td><td>Per-block AI action registry consumed by <code>SlashMenu</code>, <code>SelectionAIBar</code>, and the right-click block menu</td></tr><tr><td><code>InlineAIStream</code></td><td>Streaming display primitive: accent underline + <code>Thinking…</code> pill + Stop chip</td></tr></tbody></table><h2 id="_9-open-follow-up" tabindex="-1">9. Open follow-up <a class="header-anchor" href="#_9-open-follow-up" aria-label="Permalink to &quot;9. Open follow-up&quot;">​</a></h2><ul><li><strong>Diff mode for code blocks.</strong> Inline AI on a code block almost certainly needs diff output (red/green) like Cursor / Copilot. Out of v0 scope but the <code>NodeAIRegistry</code> should be designed so a code node can swap the accept bar for a diff variant later.</li><li><strong>Multi-block selection.</strong> Selecting across blocks (e.g. three paragraphs + a list) and asking AI to summarize is the strongest &quot;reduce&quot; use case but visually the trickiest — the inline accept bar&#39;s anchor point is ambiguous. Defer to v0.1; for v0, multi-block selection routes to the chat panel (&quot;Open in chat&quot; path) rather than inline.</li><li><strong>Local model latency.</strong> Inline AI&#39;s &quot;no panel context switch&quot; speed promise depends on first-token-time. With local Ollama backends the latency may be punishing. The <code>Thinking…</code> pill is the budgeted slot, but if backends commonly hit &gt; ~1.5s pre-token, we may want a different invocation gate — e.g. requiring an explicit ⌘Enter to start rather than auto-streaming on action pick. Decision deferred to integration testing.</li><li><strong>Refero coverage gaps.</strong> No clean reference exists for the <em>node-typed AI registry</em> pattern; we synthesized from Medium&#39;s alt-text modal + Raster&#39;s image-description product + general right-click-menu convention. Worth revisiting once Cursor / Notion add more public node-typed AI patterns.</li><li><strong><code>⌘J</code> collision check.</strong> Confirm <code>⌘J</code> isn&#39;t already bound by the OS or another Soma surface before locking. Fallback: <code>⌘.</code> (Cursor&#39;s binding).</li></ul><hr><p><em>Next pass: promote §7 pattern locks and §8 addendum wording into ADR-0005 §11 (amendment) + new §13. Component scaffolding for <code>SelectionAIBar</code>, <code>InlineAIAcceptBar</code>, <code>NodeAIRegistry</code>, <code>InlineAIStream</code> follows the existing storybook-first cadence in PRD §7 step 5.</em></p>',73)])])}const m=t(r,[["render",i]]);export{u as __pageData,m as default};
