import{_ as s,o as t,c as a,a2 as i}from"./chunks/framework.BDCokyix.js";const k=JSON.parse('{"title":"Testing","description":"","frontmatter":{},"headers":[],"relativePath":"development/testing.md","filePath":"development/testing.md","lastUpdated":1780254295000}'),o={name:"development/testing.md"};function r(n,e,h,l,p,d){return t(),a("div",null,[...e[0]||(e[0]=[i(`<h1 id="testing" tabindex="-1">Testing <a class="header-anchor" href="#testing" aria-label="Permalink to &quot;Testing&quot;">​</a></h1><p>The desktop workspace ships three test surfaces. The <code>test.yml</code> GitHub workflow runs the first two on every PR; the third is opt-in locally and gates the gh-pages deploy.</p><h2 id="unit-tests-with-coverage" tabindex="-1">Unit tests with coverage <a class="header-anchor" href="#unit-tests-with-coverage" aria-label="Permalink to &quot;Unit tests with coverage&quot;">​</a></h2><p><code>@soma/ui</code>, <code>@soma/editor</code>, and the <code>soma</code> renderer use <a href="https://vitest.dev" target="_blank" rel="noreferrer">Vitest</a> under jsdom with <code>@vitest/coverage-v8</code>. Coverage reports land in <code>&lt;package&gt;/coverage/</code> (HTML + lcov + <code>coverage-summary.json</code>) and CI uploads them as artifacts.</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">just</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> desktop-test-unit</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">               # all three packages, coverage on</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pnpm</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --filter</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> @soma/ui</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">     run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> test</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  # @soma/ui only, no coverage</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pnpm</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --filter</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> @soma/editor</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> test</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  # @soma/editor only (NodeAIRegistry…)</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pnpm</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --filter</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> soma</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">         run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> test</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  # soma only, no coverage</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br></div></div><p><code>@soma/editor</code> covers <code>createDefaultAIRegistry</code> — register, resolve by node type and surface, the locked category order (rewrite → transform → custom), and dispatch behavior including the &quot;skip insertion when response not done / content blank / range missing&quot; branches that the cutover 5d review surfaced.</p><p><code>@soma/ui</code> reuses the Storybook preview annotations (<a href="https://storybook.js.org/docs/writing-tests/portable-stories-vitest" target="_blank" rel="noreferrer">decorators + parameters</a>) via <code>setProjectAnnotations</code> in <code>vitest.setup.ts</code>, so every story file is testable through <code>composeStories(...)</code> — the same Intl, density, theme, and memory-router context that runs in Storybook. A smoke harness in <code>src/stories/__tests__/stories-smoke.test.tsx</code> renders the curated set; new stories drop in by adding an <code>import * as Stories from &quot;...&quot;</code> line.</p><h2 id="ui-e2e-cucumber-×-playwright" tabindex="-1">UI E2E (Cucumber × Playwright) <a class="header-anchor" href="#ui-e2e-cucumber-×-playwright" aria-label="Permalink to &quot;UI E2E (Cucumber × Playwright)&quot;">​</a></h2><p><code>desktop/desktop-e2e/</code> holds Gherkin features + step definitions, compiled to Playwright specs by <a href="https://vitalets.github.io/playwright-bdd/" target="_blank" rel="noreferrer"><code>playwright-bdd</code></a>. The default target is the @soma/ui Storybook — same artifact we publish to <a href="https://soma.vaam.store/storybook/" target="_blank" rel="noreferrer"><code>/storybook/</code></a> on gh-pages. CI builds the catalog, serves the static export, and runs the suite against it before publish.</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">just</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> desktop-test-e2e</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">                                   # local: boots storybook + runs</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">E2E_BASE_URL</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">https://soma.vaam.store/storybook</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> \\</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">    pnpm</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --filter</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> @soma/e2e</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> test</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">                    # against the live mirror</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br></div></div><p>See <code>desktop/desktop-e2e/README.md</code> for the step library + how to add a new feature.</p><h2 id="storybook-on-github-pages" tabindex="-1">Storybook on GitHub Pages <a class="header-anchor" href="#storybook-on-github-pages" aria-label="Permalink to &quot;Storybook on GitHub Pages&quot;">​</a></h2><p><code>just docs-build</code> builds VitePress into <code>./site</code> <em>and</em> the @soma/ui Storybook into <code>./site/storybook</code>. The <code>release-pages.yml</code> workflow publishes that directory as the <code>gh-pages</code> branch, served under the <code>soma.vaam.store</code> cname. The result is two routes under one host:</p><ul><li><code>https://soma.vaam.store/</code> — VitePress docs</li><li><code>https://soma.vaam.store/storybook/</code> — Storybook catalog</li></ul><p>The VitePress top-nav has a Storybook link; reviewers can poke the locked v0 surfaces in the browser without checking out the repo.</p>`,15)])])}const g=s(o,[["render",r]]);export{k as __pageData,g as default};
