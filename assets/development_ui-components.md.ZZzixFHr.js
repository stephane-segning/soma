import{_ as s,o as i,c as a,a2 as t}from"./chunks/framework.BDCokyix.js";const k=JSON.parse('{"title":"","description":"","frontmatter":{},"headers":[],"relativePath":"development/ui-components.md","filePath":"development/ui-components.md","lastUpdated":1780254295000}'),n={name:"development/ui-components.md"};function o(l,e,r,d,h,p){return i(),a("div",null,[...e[0]||(e[0]=[t(`<h2 id="ui-components-and-storybook" tabindex="-1">UI Components and Storybook <a class="header-anchor" href="#ui-components-and-storybook" aria-label="Permalink to &quot;UI Components and Storybook&quot;">​</a></h2><p><code>@soma/ui</code> is the shared component package — see <a href="./../architecture/prd/ui-revamp-v0-scaffold">docs/src/architecture/prd/ui-revamp-v0-scaffold.md</a> for the build plan and <a href="./../architecture/adrs/0005-ui-revamp-v0">ADR-0005</a> for the locked architecture.</p><h3 id="foundation-wave-0" tabindex="-1">Foundation (Wave 0) <a class="header-anchor" href="#foundation-wave-0" aria-label="Permalink to &quot;Foundation (Wave 0)&quot;">​</a></h3><p>The token sweep + foundation primitives ship before any component work. Every new component depends on them.</p><h4 id="design-tokens-rem-based-accessibility-aware" tabindex="-1">Design tokens (rem-based, accessibility-aware) <a class="header-anchor" href="#design-tokens-rem-based-accessibility-aware" aria-label="Permalink to &quot;Design tokens (rem-based, accessibility-aware)&quot;">​</a></h4><p>Defined in <a href="../../../desktop/desktop-ui/src/styles.css"><code>@soma/ui</code> styles.css</a>:</p><ul><li><strong>Font sizes</strong>: Use Tailwind&#39;s native <code>text-sm</code> (14px) for body + dense UI rows and <code>text-xs</code> (12px) for caps / hint text. The earlier <code>text-body / text-ui-sm / text-ui-xs</code> custom tokens were removed — they targeted exact 14 / 13 / 11px sizes from a density audit, but tailwind-merge silently dropped them when combined with <code>text-{color}</code> classes (the active-row &quot;zoom on hover&quot; bug). The 1px bumps on the densest surfaces are acceptable in exchange for the simpler model.</li><li><strong>Row height tiers</strong>: <code>row-text</code> (2rem / 32px — text-only / icon-leading), <code>row-avatar</code> (2.5rem / 40px — avatar-leading rows), <code>row-card</code> (3.25rem / 52px — two-line content). <code>row-text</code> does <strong>not</strong> fit a 40px avatar — use <code>row-avatar</code> in that case.</li><li><strong>Single shadow token</strong>: <code>--shadow-elevated</code> is the only allowed box-shadow, reserved for modal + popup window surfaces. <strong>Shadows are off by default everywhere</strong> — surfaces opt in via the <code>shadow-elevated</code> class.</li><li><strong>Surface utilities</strong>: <code>surface-card</code> (border-only resting surface), <code>glass-panel</code> (translucent + backdrop blur, <strong>no shadow</strong>). Floating overlays that genuinely need depth (modal, command-palette, bubble-toolbar, context-menu, toast) add <code>shadow-elevated</code> explicitly. The <code>*-legacy</code> variants preserve pre-revamp depth and are deleted after cutover completes — see the scaffold doc §5.</li></ul><h4 id="densityprovider-primitives-density-provider-tsx" tabindex="-1"><code>DensityProvider</code> (<a href="./../../../desktop/desktop-ui/src/components/primitives/density-provider.tsx">primitives/density-provider.tsx</a>) <a class="header-anchor" href="#densityprovider-primitives-density-provider-tsx" aria-label="Permalink to &quot;\`DensityProvider\` ([primitives/density-provider.tsx](../../../desktop/desktop-ui/src/components/primitives/density-provider.tsx))&quot;">​</a></h4><p>React context exposing <code>&#39;dense&#39; | &#39;cozy&#39; | &#39;oversized&#39;</code>. v0 ships dense; cozy and oversized are post-v0 config flips. Read via <code>useDensity()</code> or <code>useDensityValue({...})</code>.</p><h4 id="i18n-harness-i18n" tabindex="-1">i18n harness (<a href="./../../../desktop/desktop-ui/src/i18n/">i18n/</a>) <a class="header-anchor" href="#i18n-harness-i18n" aria-label="Permalink to &quot;i18n harness ([i18n/](../../../desktop/desktop-ui/src/i18n/))&quot;">​</a></h4><p><code>@soma/ui</code> ships an English-only catalog at v0; the harness exists so adding locales later is config, not refactor. Every user-facing string in new components must route through this — see Acceptance §4.7 in the scaffold.</p><div class="language-tsx vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">tsx</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { useT } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;@soma/ui/i18n/use-t&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> t</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> useT</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  &lt;</span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">button</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    {</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">t</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      id: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;bot-list.add&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      defaultMessage: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Add bot&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    })}</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  &lt;/</span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">button</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br></div></div><p>ICU placeholders work: <code>{count, plural, one {} other {}}</code>, <code>{name}</code>, etc. The <code>defaultMessage</code> is the source of truth at v0; a future extractor harvests <code>id</code> + <code>defaultMessage</code> into per-locale catalogs.</p><h3 id="storybook-conventions" tabindex="-1">Storybook conventions <a class="header-anchor" href="#storybook-conventions" aria-label="Permalink to &quot;Storybook conventions&quot;">​</a></h3><p>Stories live at <code>desktop/desktop-ui/src/stories/&lt;component&gt;.stories.tsx</code>. Run locally:</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pnpm</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --filter</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> @soma/ui</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> storybook</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>Every new component must ship with a story exercising the full state matrix:</p><ul><li><strong>default</strong> (resting)</li><li><strong>hover</strong></li><li><strong>focus-visible</strong> (keyboard focus)</li><li><strong>disabled</strong> (where applicable)</li><li><strong>empty</strong> (where applicable — use the <a href="./../../../desktop/desktop-ui/src/components/primitives"><code>Empty</code> primitive</a> variant matching the surface)</li><li><strong>error</strong> (where applicable — inline error rendering, never toast-only; see <a href="./../architecture/adrs/0005-ui-revamp-v0">ADR-0005 §6</a>)</li><li><strong>dark theme</strong> — set <code>parameters: { theme: &#39;luxury&#39; }</code> on a story for explicit dark coverage.</li></ul><p>For list rows / popovers / menus, also: <strong>keyboard navigation</strong> (arrow / enter / esc).</p><p>Stories are wrapped automatically (in <code>.storybook/preview.tsx</code>) with:</p><ul><li><code>SomaIntlProvider</code> — <code>useT()</code> resolves inside any story</li><li><code>DensityProvider</code> — defaults to <code>dense</code>; override per story with <code>parameters: { density: &#39;cozy&#39; }</code></li><li>A <code>data-theme</code> decorator that sets DaisyUI&#39;s theme (<code>cmyk</code> default)</li><li>A memory router</li></ul><p>There is <strong>no hardcoded canvas background</strong> — <code>bg-base-100</code> (theme-driven) carries the surface. Stories that need a different canvas declare it via <code>parameters</code>.</p><h3 id="conventions-locked-after-the-editor-polish-pr-cycle" tabindex="-1">Conventions (locked after the editor-polish PR cycle) <a class="header-anchor" href="#conventions-locked-after-the-editor-polish-pr-cycle" aria-label="Permalink to &quot;Conventions (locked after the editor-polish PR cycle)&quot;">​</a></h3><p>Every new component should follow these rules. They came out of a long testing loop (see the Pitfalls section below) and exist to keep the visual language coherent.</p><ol><li><p><strong>Lean on daisyUI 5 primitives before hand-rolling.</strong> Before adding a flex/grid layout, check whether daisyUI ships the shape already (<code>.btn</code>, <code>.badge</code>, <code>.list</code>, <code>.list-row</code>, <code>.kbd</code>, <code>.dropdown</code>, <code>.menu</code>, <code>.select</code>, <code>.input</code>, <code>.loading</code>, <code>.status</code>). The reference is daisyUI&#39;s <a href="https://daisyui.com/llms.txt" target="_blank" rel="noreferrer">llms.txt</a>. We migrated Pill → <code>.badge .badge-soft</code>, the code-block language picker → <code>.select select-ghost</code>, DenseRow → <code>.list-row</code>, BotList wrapper → <code>&lt;ul class=&quot;list&quot;&gt;</code>. Three migrations we <em>considered</em> and rejected (with a comment in the source explaining why): MenuShell → daisy <code>.menu</code> (would re-introduce a 200ms transition on every row); DenseRow&#39;s grid → daisy positional <code>list-row</code> columns (our API is named-slot, daisy&#39;s is positional — only the surface migrates).</p><p><strong>Density override (<code>.list-dense</code>).</strong> daisy&#39;s <code>.list-row</code> defaults to <code>padding: 1rem</code> and <code>gap: 1rem</code>, which lands at ~56-60 px row height — appropriate for marketing surfaces, not for sidebars / file browsers / panel contents. Always pair <code>.list</code> with our <code>.list-dense</code> modifier (defined in <code>styles.css</code>) on dense surfaces; it tightens the row padding to ~5×10 px and drops the per-row corner-rounding. <code>DenseRow</code> documents this requirement in its header; <code>BotList</code> applies it automatically.</p></li><li><p><strong>Tailwind utilities only — no custom <code>text-*</code> tokens.</strong> We removed <code>text-body / text-ui-sm / text-ui-xs</code>. Use <code>text-sm</code> for body + dense rows, <code>text-xs</code> for caps / hint text. Adding custom font-size tokens means teaching <code>tailwind-merge</code> about them or eating the silent-drop bug described in Pitfalls.</p></li><li><p><strong>Row-list highlights snap.</strong> Never <code>transition-colors</code> on a row primitive&#39;s background. The 150 ms colour fade reads as the row &quot;growing into place&quot; as the mouse moves across the list (the symptom users repeatedly reported as &quot;zoom on hover&quot;). Snap active/hover background changes — the cursor motion supplies the feedback.</p></li><li><p><strong>No <code>scale: 0.96 → 1</code> entry animations on popovers.</strong> A pop-in scale of even 4 % looks identical to a hover-zoom when the popover appears as a hover/selection consequence. Slash menu, action menu, format bubble, command palette, window-chrome buttons — all dropped during the polish pass. Keep <code>opacity</code> / <code>y</code> / <code>x</code> transitions where you need a fade in.</p></li><li><p><strong>Daisy classes for keyboard shortcuts.</strong> Render every keyboard key through the <code>Kbd</code> primitive (which wraps <code>&lt;kbd class=&quot;kbd&quot;&gt;</code>). It accepts a single key, an array, or a string that auto-splits (graphemes for <code>&quot;⌘⇧F&quot;</code>, <code>+</code>-split for <code>&quot;Ctrl+Shift+Del&quot;</code>). The chord shape uses bare <code>+</code> text nodes between sibling <code>&lt;kbd&gt;</code>s — the exact shape from daisy&#39;s docs.</p></li><li><p><strong>Compose, don&#39;t fight, the <code>cn()</code> helper.</strong> Our <code>utils/cn.ts</code> pipes through <code>tailwind-merge</code>. Don&#39;t introduce custom utilities that look like Tailwind&#39;s grouped classes (e.g. anything named <code>text-*</code>, <code>bg-*</code>, <code>border-*</code>) without registering them in the <code>extendTailwindMerge</code> config — twMerge will silently drop one of them on conflict.</p></li><li><p><strong>Side panels: chip-bar lives in main, rail is a pure panel stack.</strong> The collapsed-panel switcher (<code>PanelChipBar</code>) goes into <code>DesktopShell</code>&#39;s <code>mainTopLeft</code> / <code>mainTopRight</code> slots — <em>not</em> inside the rail. When the rail beside it opens, <code>&lt;main&gt;</code> shrinks and the chip bar rides along automatically. The rail itself is only ever a <code>PanelContainer</code> filtered by <code>expandedIds</code>. The two pieces of state — &quot;what&#39;s in the bar&quot; and &quot;what&#39;s in the rail&quot; — are derived from the same <code>expandedIds: Set&lt;string&gt;</code> the caller owns. See the <a href="#side-panels-chip-bar-rail-composition">Side panels</a> section for the full wiring.</p></li></ol><h3 id="primitives-catalog" tabindex="-1">Primitives catalog <a class="header-anchor" href="#primitives-catalog" aria-label="Permalink to &quot;Primitives catalog&quot;">​</a></h3><p>Quick reference for the reusable bits in <code>@soma/ui</code>. Click into the source for the full prop surface.</p><table tabindex="0"><thead><tr><th>Primitive</th><th>Where</th><th>One-liner</th></tr></thead><tbody><tr><td><code>Kbd</code></td><td><a href="./../../../desktop/desktop-ui/src/components/primitives/kbd.tsx"><code>components/primitives/kbd.tsx</code></a></td><td>daisyUI <code>&lt;kbd class=&quot;kbd&quot;&gt;</code> with size variants + chord helper. Accepts string / array / ReactNode.</td></tr><tr><td><code>Pill</code></td><td><a href="./../../../desktop/desktop-ui/src/components/primitives/pill.tsx"><code>components/primitives/pill.tsx</code></a></td><td>daisyUI <code>.badge badge-soft</code> + optional dot (<code>true</code> / <code>&quot;pulse&quot;</code>) for status chips.</td></tr><tr><td><code>MenuShell</code> / <code>MenuItem</code> / <code>MenuSectionLabel</code></td><td><a href="./../../../desktop/desktop-ui/src/components/overlays/menu-shell.tsx"><code>components/overlays/menu-shell.tsx</code></a></td><td>Shared shell for popover menus (slash, context, AI bar action list, add-block menu). Rows snap on hover — no transition.</td></tr><tr><td><code>DenseRow</code></td><td><a href="./../../../desktop/desktop-ui/src/components/lists/dense-row.tsx"><code>components/lists/dense-row.tsx</code></a></td><td>daisy <code>&lt;li class=&quot;list-row&quot;&gt;</code> with named slots (<code>leading</code> / <code>primary</code> + <code>sub</code> / <code>status</code> / <code>meta</code> / <code>actions</code>). Must be inside <code>&lt;ul class=&quot;list list-dense&quot;&gt;</code> — without <code>list-dense</code> the row picks up daisy&#39;s airy 1 rem padding and stops looking dense.</td></tr><tr><td><code>Panel</code></td><td><a href="./../../../desktop/desktop-ui/src/components/panels/panel.tsx"><code>components/panels/panel.tsx</code></a></td><td>A single floating-card panel (header + body + footer). Header carries the <code>−</code> collapse button (sends the panel back to its <code>PanelChipBar</code>) and the <code>×</code> close button.</td></tr><tr><td><code>PanelStack</code></td><td><a href="./../../../desktop/desktop-ui/src/components/panels/panel-stack.tsx"><code>components/panels/panel-stack.tsx</code></a></td><td>Vertical full-width stack of <code>Panel</code>s with <code>p-2 gap-2</code>. Returns <code>null</code> if empty.</td></tr><tr><td><code>PanelChip</code> / <code>PanelChipBar</code></td><td><a href="./../../../desktop/desktop-ui/src/components/panels/panel-chip-bar.tsx"><code>components/panels/panel-chip-bar.tsx</code></a></td><td>Floating switcher in main&#39;s top-corner. <strong>No card chrome</strong> — only a <code>backdrop-blur-md</code> clipped to a rounded shape, no border or shadow or opaque fill. Renders one icon per <strong>collapsed</strong> panel; clicking expands the panel into the rail (the chip then disappears). Goes into <code>&lt;DesktopShell mainTopLeft={…}&gt;</code> / <code>mainTopRight={…}</code> slots.</td></tr><tr><td><code>PanelContainer</code></td><td><a href="./../../../desktop/desktop-ui/src/components/panels/panel-container.tsx"><code>components/panels/panel-container.tsx</code></a></td><td>Thin composition over <code>PanelStack</code>. Filters the panel inventory by <code>expandedIds</code> and renders the visible ones at 100 % rail width. <strong>No</strong> chip strip, <strong>no</strong> multi-column, <strong>no</strong> horizontal scroll. Callers leave the matching rail&#39;s <code>content</code> prop <code>null</code> when nothing is expanded so the rail unmounts.</td></tr><tr><td><code>BackendSwitcher</code></td><td><a href="./../../../desktop/desktop-ui/src/components/chat/backend-switcher.tsx"><code>components/chat/backend-switcher.tsx</code></a></td><td>Full <code>@floating-ui/react</code> dropdown for picking the active ACP backend in the composer. Replaces the deleted <code>AiModelSelector</code>.</td></tr><tr><td><code>BotList</code></td><td><a href="./../../../desktop/desktop-ui/src/components/lists/bot-list.tsx"><code>components/lists/bot-list.tsx</code></a></td><td><code>&lt;ul class=&quot;list bg-base-100&quot;&gt;</code> of <code>DenseRow</code>s, one per bot, with <code>FailureRow</code> as a sibling <code>&lt;li&gt;</code> for failed bots.</td></tr><tr><td><code>SelectionBubble</code> / <code>SelectionAIBar</code></td><td><a href="./../../../desktop/desktop-ui/src/components/editor/"><code>components/editor/</code></a></td><td>Floating editor toolbars. SelectionAIBar dismisses on Escape + click-outside (caught a stale-popup bug).</td></tr></tbody></table><h3 id="flagship-preview" tabindex="-1">Flagship preview <a class="header-anchor" href="#flagship-preview" aria-label="Permalink to &quot;Flagship preview&quot;">​</a></h3><p><code>Desktop / Shell → Soma App</code> in Storybook composes every primitive in the library into the shape of the real renderer: header with backend switcher and <code>Kbd</code> quick-open hint, two <code>PanelChipBar</code>s (top-left: Pages + Outline; top-right: Chat / Bots / Page history / Agenda), two <code>PanelContainer</code>s (one per rail, mounted only when their <code>expandedIds</code> set is non-empty), and a Tiptap-style editor mock in main. Treat that story as the visual-regression canary for the whole shell — if something looks off there it&#39;s almost always off in the actual renderer too.</p><h3 id="side-panels-chip-bar-rail-composition" tabindex="-1">Side panels: chip-bar + rail composition <a class="header-anchor" href="#side-panels-chip-bar-rail-composition" aria-label="Permalink to &quot;Side panels: chip-bar + rail composition {#side-panels-chip-bar-rail-composition}&quot;">​</a></h3><p>The side-panel system is built from four cooperating primitives that share <strong>one</strong> piece of state (<code>expandedIds: Set&lt;string&gt;</code> per side). The split is the contract you have to understand before touching anything in <code>components/panels/</code> or <code>components/layout/</code>.</p><h4 id="mental-model" tabindex="-1">Mental model <a class="header-anchor" href="#mental-model" aria-label="Permalink to &quot;Mental model&quot;">​</a></h4><ul><li><strong><code>PanelChipBar</code></strong> is a floating switcher pinned to the top-left or top-right corner of <code>&lt;main&gt;</code>. It renders one icon per <strong>collapsed</strong> panel — never one per expanded panel. Clicking a chip calls <code>onExpand(id)</code>; the caller adds the id to its <code>expandedIds</code> set; on the next render the chip vanishes (because it&#39;s no longer collapsed) and the matching panel appears in the rail.</li><li><strong><code>PanelContainer</code></strong> is the rail-side host. It takes the full inventory plus the same <code>expandedIds</code> set, filters the inventory to the expanded subset, and stacks those panels at 100 % rail width via <code>PanelStack</code>. Returns <code>null</code> when nothing is expanded.</li><li><strong><code>Panel</code></strong> is the rounded floating card. Its header has a <code>−</code> button which fires <code>onCollapse(id)</code> — the caller removes the id from <code>expandedIds</code>; on the next render the panel disappears from the rail and the chip reappears in the bar.</li><li><strong><code>ShellPanel</code></strong> (the rail wrapper inside <code>DesktopShell</code>) returns <code>null</code> when its <code>content</code> prop is <code>null</code>. So when every panel on a side is collapsed, the caller passes <code>null</code> as the rail&#39;s column and the rail unmounts entirely; width returns to 0; persisted width is restored on the next mount.</li></ul><p>The reason the bar lives in main, not in the rail, is exactly so the rail can unmount. Coupling the bar to the rail was the root cause of the &quot;right rail can&#39;t auto-shrink&quot; bug — the rail had to stay open just to host the bar.</p><h4 id="canonical-wiring" tabindex="-1">Canonical wiring <a class="header-anchor" href="#canonical-wiring" aria-label="Permalink to &quot;Canonical wiring&quot;">​</a></h4><div class="language-tsx vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">tsx</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// Build-time panel inventories. Move a panel L↔R by shifting it</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// between these two arrays — that&#39;s the only &quot;configuration&quot; needed.</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> LEFT_PANELS</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> PanelDescriptor</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[]  </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> [pagesPanel, outlinePanel];</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> RIGHT_PANELS</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> PanelDescriptor</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[] </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> [chatPanel, botsPanel, historyPanel, agendaPanel];</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">function</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Workspace</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">() {</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  // One Set per side, owned by the app. Both PanelChipBar and</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  // PanelContainer read from this — that&#39;s how the two stay in sync.</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  const</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> [</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">leftExpanded</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,  </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">setLeftExpanded</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]  </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> useState</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">Set</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;&gt;(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">new</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Set</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">([</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;pages&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]));</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  const</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> [</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">rightExpanded</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">setRightExpanded</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">] </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> useState</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">Set</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;&gt;(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">new</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Set</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">([</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;chat&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]));</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  const</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> expand</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">   =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">set</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Set</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">id</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> new</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Set</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(set).</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">add</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(id);</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  const</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> collapse</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">set</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Set</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">id</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> n</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> new</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Set</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(set); n.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">delete</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(id); </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> n; };</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    &lt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">DesktopShell</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">      mainTopLeft</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        &lt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">PanelChipBar</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">          panels</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">LEFT_PANELS</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">          expandedIds</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{leftExpanded}</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">          onExpand</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{(</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">id</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> setLeftExpanded</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">((</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">p</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> expand</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(p, id))}</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">          placement</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;top-left&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        /&gt;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      }</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">      mainTopRight</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        &lt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">PanelChipBar</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">          panels</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">RIGHT_PANELS</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">          expandedIds</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{rightExpanded}</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">          onExpand</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{(</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">id</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> setRightExpanded</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">((</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">p</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> expand</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(p, id))}</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">          placement</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;top-right&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        /&gt;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      }</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">      // \`null\` when no panels expanded → rail unmounts → width returns</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">      // to 0 → next mount restores persisted width.</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">      leftColumn</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        leftExpanded.size </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 0</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> ?</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">          &lt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">PanelContainer</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">            panels</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">LEFT_PANELS</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">            expandedIds</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{leftExpanded}</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">            onCollapse</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{(</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">id</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> setLeftExpanded</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">((</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">p</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> collapse</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(p, id))}</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">          /&gt;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        ) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> null</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      }</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">      rightColumn</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        rightExpanded.size </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 0</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> ?</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">          &lt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">PanelContainer</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">            panels</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">RIGHT_PANELS</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">            expandedIds</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{rightExpanded}</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">            onCollapse</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{(</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">id</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> setRightExpanded</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">((</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">p</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> collapse</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(p, id))}</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">          /&gt;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        ) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> null</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    &gt;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      &lt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Editor</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> /&gt;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    &lt;/</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">DesktopShell</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  );</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br><span class="line-number">13</span><br><span class="line-number">14</span><br><span class="line-number">15</span><br><span class="line-number">16</span><br><span class="line-number">17</span><br><span class="line-number">18</span><br><span class="line-number">19</span><br><span class="line-number">20</span><br><span class="line-number">21</span><br><span class="line-number">22</span><br><span class="line-number">23</span><br><span class="line-number">24</span><br><span class="line-number">25</span><br><span class="line-number">26</span><br><span class="line-number">27</span><br><span class="line-number">28</span><br><span class="line-number">29</span><br><span class="line-number">30</span><br><span class="line-number">31</span><br><span class="line-number">32</span><br><span class="line-number">33</span><br><span class="line-number">34</span><br><span class="line-number">35</span><br><span class="line-number">36</span><br><span class="line-number">37</span><br><span class="line-number">38</span><br><span class="line-number">39</span><br><span class="line-number">40</span><br><span class="line-number">41</span><br><span class="line-number">42</span><br><span class="line-number">43</span><br><span class="line-number">44</span><br><span class="line-number">45</span><br><span class="line-number">46</span><br><span class="line-number">47</span><br><span class="line-number">48</span><br><span class="line-number">49</span><br><span class="line-number">50</span><br><span class="line-number">51</span><br><span class="line-number">52</span><br><span class="line-number">53</span><br><span class="line-number">54</span><br><span class="line-number">55</span><br><span class="line-number">56</span><br><span class="line-number">57</span><br></div></div><h4 id="build-time-left-right-placement" tabindex="-1">Build-time left/right placement <a class="header-anchor" href="#build-time-left-right-placement" aria-label="Permalink to &quot;Build-time left/right placement&quot;">​</a></h4><p>There is <strong>no runtime drag-and-drop</strong> between rails and <strong>no per-panel <code>side</code> prop</strong>. The side a panel lives on is whichever array (<code>LEFT_PANELS</code> vs <code>RIGHT_PANELS</code>) the developer puts the descriptor in. To move a panel L→R, shift one line of code. This keeps panel identity / state colocated with the array it belongs to and avoids a class of state-loss bugs that drag-and-drop reordering would introduce.</p><h4 id="resize-bounds-lightly-resizable-left-freer-right" tabindex="-1">Resize bounds (lightly-resizable left, freer right) <a class="header-anchor" href="#resize-bounds-lightly-resizable-left-freer-right" aria-label="Permalink to &quot;Resize bounds (lightly-resizable left, freer right)&quot;">​</a></h4><p><code>DesktopShell</code> exposes per-side <code>{left,right}{Min,Max}Width</code> props with defaults that match the typical content density of each side:</p><table tabindex="0"><thead><tr><th>Side</th><th>Default min</th><th>Default max</th><th>Rationale</th></tr></thead><tbody><tr><td>Left</td><td>200 px</td><td>320 px</td><td>Pages / outlines / nav are dense enough that more width adds little.</td></tr><tr><td>Right</td><td>280 px</td><td>720 px</td><td>Chat / inspector / tools want more room for messages and forms.</td></tr></tbody></table><p>The <code>ResizeHandle</code> is invisible at rest; it only paints a 2 × 40 px primary-tinted pill on hover (opacity-fades in, no transform) and solidifies on active drag. There is <strong>no border-l / border-r</strong> on either rail — the only visible separator is that hover pill. This is the explicit fix for the &quot;static hairline cuts through floating-card gutters&quot; complaint that triggered the chip-bar rewrite.</p><h4 id="when-to-bypass-panelcontainer" tabindex="-1">When to bypass <code>PanelContainer</code> <a class="header-anchor" href="#when-to-bypass-panelcontainer" aria-label="Permalink to &quot;When to bypass \`PanelContainer\`&quot;">​</a></h4><p><code>PanelStack</code> (the vertical full-width Panel column) is exported on its own for stories or surfaces that just want the stack without the <code>expandedIds</code> filter. Likewise <code>Panel</code> is usable in isolation if you need a single floating card with no rail-side logic. The composition hierarchy is intentionally:</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Panel  ⊂  PanelStack  ⊂  PanelContainer</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>— each layer adds one concern. Don&#39;t reach below the level you need; don&#39;t reach above it either.</p><h3 id="pitfalls-the-bugs-that-cost-us-multiple-rounds" tabindex="-1">Pitfalls (the bugs that cost us multiple rounds) <a class="header-anchor" href="#pitfalls-the-bugs-that-cost-us-multiple-rounds" aria-label="Permalink to &quot;Pitfalls (the bugs that cost us multiple rounds)&quot;">​</a></h3><p>Logged here so they don&#39;t recur silently.</p><h4 id="tailwind-merge-custom-text-size-tokens-silent-drop" tabindex="-1"><code>tailwind-merge</code> + custom text-size tokens = silent drop <a class="header-anchor" href="#tailwind-merge-custom-text-size-tokens-silent-drop" aria-label="Permalink to &quot;\`tailwind-merge\` + custom text-size tokens = silent drop&quot;">​</a></h4><p>Composing a custom font-size class with a colour class through <code>cn()</code> silently dropped the size. tailwind-merge&#39;s default config doesn&#39;t know <code>text-ui-sm</code> is a font-size, so when it saw <code>cn(&quot;text-ui-sm&quot;, &quot;text-primary&quot;)</code> it grouped both as conflicting <code>text-*</code> tokens and kept only <code>text-primary</code>. The active row jumped from 13 px to the browser default 16 px — the visible &quot;zoom on hover&quot; the user reported repeatedly. Fix shipped two ways: short-term, an <code>extendTailwindMerge</code> config registering the custom group; long-term, drop the custom tokens entirely and use Tailwind-native <code>text-sm</code> / <code>text-xs</code>. If a new custom utility looks like a Tailwind group prefix (<code>text-*</code>, <code>bg-*</code>, <code>border-*</code>, …), register it explicitly or expect silent drops.</p><h4 id="transition-colors-on-row-list-backgrounds-reads-as-zoom" tabindex="-1"><code>transition-colors</code> on row-list backgrounds reads as zoom <a class="header-anchor" href="#transition-colors-on-row-list-backgrounds-reads-as-zoom" aria-label="Permalink to &quot;\`transition-colors\` on row-list backgrounds reads as zoom&quot;">​</a></h4><p>Animating a row&#39;s bg-color from transparent → <code>bg-base-200</code> over 150 ms made the highlighted row&#39;s coloured rectangle visibly fade in, which the user perceived as the row growing in. Every row-style list primitive (MenuItem, BackendSwitcher row, CommandPalette row, MentionPicker, TreePopover, DenseRow) had to be swept. Rule: never animate the background colour on a row&#39;s hover/active state. Snap instantly.</p><h4 id="scale-0-96-→-1-entry-animations-on-hover-triggered-popovers" tabindex="-1"><code>scale: 0.96 → 1</code> entry animations on hover-triggered popovers <a class="header-anchor" href="#scale-0-96-→-1-entry-animations-on-hover-triggered-popovers" aria-label="Permalink to &quot;\`scale: 0.96 → 1\` entry animations on hover-triggered popovers&quot;">​</a></h4><p>Even a 4 % scale-in animation reads as a hover-zoom when the popover opens because the user is hovering. Killed across the action menu, context menu, command palette, bubble toolbar, quick-action panel, window-chrome traffic-light buttons. The <a href="../../../desktop/desktop-ui/src/components/overlays/no-scale-animations.test.ts"><code>no-scale-animations.test.ts</code></a> guard pins the rule against re-introduction.</p><h4 id="draghandle-prop-identity-stability" tabindex="-1">DragHandle prop identity stability <a class="header-anchor" href="#draghandle-prop-identity-stability" aria-label="Permalink to &quot;DragHandle prop identity stability&quot;">​</a></h4><p><code>@tiptap/extension-drag-handle-react</code> puts <code>onNodeChange</code>, <code>onElementDragStart</code>, <code>onElementDragEnd</code>, and <code>computePositionConfig</code> in a <code>useEffect</code> dep list. Passing fresh-identity arrows on every render unregistered + re-registered the ProseMirror drag-handle plugin on every mouse move. Plugin re-registration reconfigures the editor&#39;s plugin list, which resets the suggestion plugin&#39;s state to <code>{ active: false }</code> — the slash menu vanished as soon as the mouse moved. Wrap every prop in <code>useCallback</code> / <code>useMemo</code>. A regression test in <a href="./../../../desktop/desktop-editor/src/menus/action-menu.test.tsx"><code>action-menu.test.tsx</code></a> pins this.</p><h4 id="list-row-is-a-positional-grid-not-named-slots" tabindex="-1"><code>list-row</code> is a positional grid, not named slots <a class="header-anchor" href="#list-row-is-a-positional-grid-not-named-slots" aria-label="Permalink to &quot;\`list-row\` is a positional grid, not named slots&quot;">​</a></h4><p>daisyUI&#39;s <code>.list-row</code> is <code>display: grid; grid-auto-flow: column;</code> — each child is its own column. If a component has named-conditional slots (some optional), the positional model fights you. Solutions: either render placeholders so positions stay stable, or mark the intended-to-grow column with <code>list-col-grow</code>. DenseRow uses the second approach. Don&#39;t try to use <code>list-row</code> for a custom slot API without confronting this first.</p><h4 id="stale-rect-closure-on-floating-ui-virtual-element" tabindex="-1">Stale rect closure on floating-ui virtual element <a class="header-anchor" href="#stale-rect-closure-on-floating-ui-virtual-element" aria-label="Permalink to &quot;Stale rect closure on floating-ui virtual element&quot;">​</a></h4><p>A <code>VirtualElement</code> whose <code>getBoundingClientRect: () =&gt; rect</code> closure-captured <code>rect</code> once at effect time, then never updated. The slash menu didn&#39;t follow scroll because <code>autoUpdate</code> kept reading the stale DOMRect. Always read the rect <em>inside</em> the getter so it re-evaluates on every floating-ui recompute:</p><div class="language-ts vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">getBoundingClientRect</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: () </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> clientRect</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">() </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">??</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> new</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> DOMRect</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>Pinned by <a href="../../../desktop/desktop-editor/src/extensions/commander/command-list-rect.test.ts"><code>command-list-rect.test.ts</code></a>.</p><h4 id="side-panel-chip-strip-coupled-to-the-rail-—-rail-can-t-auto-shrink" tabindex="-1">Side-panel chip strip coupled to the rail — rail can&#39;t auto-shrink <a class="header-anchor" href="#side-panel-chip-strip-coupled-to-the-rail-—-rail-can-t-auto-shrink" aria-label="Permalink to &quot;Side-panel chip strip coupled to the rail — rail can&#39;t auto-shrink&quot;">​</a></h4><p>The first cut of <code>PanelContainer</code> hosted both the expanded panels <em>and</em> the strip of collapsed icons inside the rail. That couples two concerns: as long as the strip exists, the rail can&#39;t unmount, so the rail&#39;s width never drops below the strip&#39;s width. Users called this out as &quot;right rail floating + too wide when nothing&#39;s expanded&quot; — the rail visibly took ~32-40 px of horizontal space just to show the collapsed-icon column.</p><p>The fix is structural, not cosmetic: move the chip strip out of the rail entirely. <code>PanelChipBar</code> lives in <code>&lt;main&gt;</code>&#39;s top corners via <code>DesktopShell.mainTopLeft</code> / <code>mainTopRight</code>; the rail becomes a pure <code>PanelContainer</code> that returns <code>null</code> when <code>expandedIds</code> is empty, which lets <code>ShellPanel</code> unmount and reclaim the width. The two components share one <code>expandedIds: Set&lt;string&gt;</code> so they stay in sync.</p><p>The general rule: if a &quot;container&quot; component hosts two pieces of UI where one needs to disappear and the other doesn&#39;t, those need to be separate components rendered by separate slots — not a single component with internal modes.</p><h4 id="always-visible-resize-divider-breaks-the-floating-card-aesthetic" tabindex="-1">Always-visible resize divider breaks the floating-card aesthetic <a class="header-anchor" href="#always-visible-resize-divider-breaks-the-floating-card-aesthetic" aria-label="Permalink to &quot;Always-visible resize divider breaks the floating-card aesthetic&quot;">​</a></h4><p>Before the chip-bar rewrite, <code>ResizeHandle</code> painted a 1 px <code>bg-base-300</code> hairline at all times, which read fine on flush sidebars but looked like a stray line cutting through the gutter once the rail switched to floating-card chrome. The fix: the handle paints <strong>nothing</strong> at rest. A 2 × 40 px primary-tinted pill opacity-fades in only on hover, and solidifies on active drag. No transform anywhere on the handle — opacity only — so the affordance can&#39;t be confused with a hover-zoom.</p><h4 id="daisyui-version-drift-across-extensions" tabindex="-1">daisyUI version drift across extensions <a class="header-anchor" href="#daisyui-version-drift-across-extensions" aria-label="Permalink to &quot;daisyUI version drift across extensions&quot;">​</a></h4><p>A <code>@tiptap/extension-highlight</code> minor bump (3.18 → 3.23) silently imported a <code>getStyleProperty</code> export from <code>@tiptap/core</code> that didn&#39;t exist in core 3.18, breaking Storybook&#39;s <code>optimizeDeps</code>. Pin extension versions in <code>~</code>-range when the rest of the toolkit is at a locked <code>^x.y.z</code>, so only patches flow in until a coordinated bump.</p><h3 id="chat-reasoning" tabindex="-1">Chat + Reasoning <a class="header-anchor" href="#chat-reasoning" aria-label="Permalink to &quot;Chat + Reasoning&quot;">​</a></h3><ul><li><code>AiConversation</code> + <code>AiMessage</code> render chat roles (user/assistant/tool/source) with optional thinking blocks.</li><li><code>AiThinking</code> renders a collapsible &quot;Thinking…&quot; section; pass <code>status=&quot;thinking&quot; | &quot;complete&quot;</code>, <code>durationLabel</code>, and <code>content</code> (markdown rendered via <code>AiMarkdown</code>).</li><li>Keep chat wrappers thin: <code>AiChat</code> provides a scroll container; inputs are kept separate.</li></ul><h3 id="grapheme-aware-typing-tapia-typing-practice" tabindex="-1">Grapheme-Aware Typing (Tapia typing practice) <a class="header-anchor" href="#grapheme-aware-typing-tapia-typing-practice" aria-label="Permalink to &quot;Grapheme-Aware Typing (Tapia typing practice)&quot;">​</a></h3><ul><li><code>useGraphemes(value: string)</code> splits text into grapheme clusters using <code>Intl.Segmenter</code> (fallback to <code>Array.from</code>).</li><li><code>CharDisplay</code> is a dumb renderer that expects <code>shouldGraphemes</code> and <code>isGraphemes</code>. It aligns glyphs (including emojis) and shows mismatches with an elevated correct glyph.</li></ul><h3 id="desktop-layout" tabindex="-1">Desktop Layout <a class="header-anchor" href="#desktop-layout" aria-label="Permalink to &quot;Desktop Layout&quot;">​</a></h3><p><code>DesktopShell</code> is the full-screen wrapper. Its prop surface, top to bottom:</p><table tabindex="0"><thead><tr><th>Slot / prop</th><th>Renders where</th></tr></thead><tbody><tr><td><code>header</code> (render-prop)</td><td>Above the body. Receives <code>{ toggleLeft, toggleRight, leftOpen, rightOpen, hasLeft, hasRight }</code> so the app owns its toolbar.</td></tr><tr><td><code>leftColumn</code> / <code>rightColumn</code></td><td>Hosted in <code>ShellPanel</code>. Returns <code>null</code> (unmounts the rail, width 0) when its column prop is <code>null</code>.</td></tr><tr><td><code>mainTopLeft</code> / <code>mainTopRight</code></td><td>Absolutely positioned inside <code>&lt;main&gt;</code> at <code>top:8 left/right:8</code>. Designed for <code>PanelChipBar</code> — when the rail beside it opens, main shrinks and the slot rides along.</td></tr><tr><td><code>children</code></td><td>The main column. <code>overflow-auto max-h-full</code>, so long content scrolls while rails stay fixed.</td></tr><tr><td><code>footer</code></td><td>Below the body.</td></tr><tr><td><code>overlays</code></td><td>A <code>pointer-events-none absolute inset-0 z-20</code> layer for global modals / drag previews.</td></tr></tbody></table><p>Per-side configuration props:</p><table tabindex="0"><thead><tr><th>Prop</th><th>Default</th></tr></thead><tbody><tr><td><code>initialLeftWidth</code> / <code>initialRightWidth</code></td><td>240 px / 320 px (overridable)</td></tr><tr><td><code>leftMinWidth</code> / <code>leftMaxWidth</code></td><td>200 / 320 (lightly resizable — pages / outlines don&#39;t gain from wider widths)</td></tr><tr><td><code>rightMinWidth</code> / <code>rightMaxWidth</code></td><td>280 / 720 (chat / inspector want more room)</td></tr><tr><td><code>onLeftResizeStop</code> / <code>onRightResizeStop</code></td><td>Fired on drag end with the clamped width — wire to a persistence layer or to <code>useState</code></td></tr><tr><td><code>storageKey</code></td><td>When set, widths + open/closed state persist to <code>localStorage</code> via the built-in storage helper</td></tr></tbody></table><p>For everything panel-related — <code>PanelChipBar</code> placement, the <code>expandedIds</code> contract, the rail unmount/remount logic, the <code>Panel ⊂ PanelStack ⊂ PanelContainer</code> composition — see the <a href="#side-panels-chip-bar-rail-composition">Side panels</a> section above. The <code>Desktop / Shell → Soma App</code> Storybook scene is the visual regression canary for any chrome-level change.</p><h3 id="window-drag-regions-tauri" tabindex="-1">Window drag regions (Tauri) <a class="header-anchor" href="#window-drag-regions-tauri" aria-label="Permalink to &quot;Window drag regions (Tauri)&quot;">​</a></h3><ul><li>The Tauri app uses a frameless window (<code>decorations: false</code>, <code>titleBarStyle: &quot;Overlay&quot;</code>, <code>hiddenTitle: true</code> in <code>tauri.conf.json</code>). Mark draggable chrome with <code>data-drag-region</code> and opt interactive elements out with <code>data-no-drag</code>.</li></ul><h3 id="build-publishing" tabindex="-1">Build + publishing <a class="header-anchor" href="#build-publishing" aria-label="Permalink to &quot;Build + publishing&quot;">​</a></h3><ul><li>Build locally: <code>pnpm --filter @soma/ui run storybook</code> (dev) or <code>pnpm --filter @soma/ui run build:storybook</code>.</li><li>Docs pipeline: <code>just docs-build</code> also builds Storybook into <code>site/storybook</code> after VitePress runs.</li></ul>`,85)])])}const g=s(n,[["render",o]]);export{k as __pageData,g as default};
