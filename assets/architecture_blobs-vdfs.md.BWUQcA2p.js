import{_ as r,C as i,o,c as d,a2 as a,b as l,w as t,a as n,E as c,a3 as p}from"./chunks/framework.BDCokyix.js";const _=JSON.parse('{"title":"Blobs and Cache-Serving Bots: Content-Addressed Storage + Fetch-by-CID","description":"","frontmatter":{},"headers":[],"relativePath":"architecture/blobs-vdfs.md","filePath":"architecture/blobs-vdfs.md","lastUpdated":1780254295000}'),b={name:"architecture/blobs-vdfs.md"};function h(m,e,u,g,f,y){const s=i("Mermaid");return o(),d("div",null,[e[1]||(e[1]=a('<h1 id="blobs-and-cache-serving-bots-content-addressed-storage-fetch-by-cid" tabindex="-1">Blobs and Cache-Serving Bots: Content-Addressed Storage + Fetch-by-CID <a class="header-anchor" href="#blobs-and-cache-serving-bots-content-addressed-storage-fetch-by-cid" aria-label="Permalink to &quot;Blobs and Cache-Serving Bots: Content-Addressed Storage + Fetch-by-CID&quot;">​</a></h1><p>This doc describes Soma&#39;s blob layer: <strong>content-addressed storage (CAS)</strong> plus a <strong>pull-based fetch-by-CID</strong> protocol over libp2p.</p><p>Human model:</p><ul><li>attachments already stored on a device stay available there locally</li><li>if another device opens an attachment for the first time, Soma may need a reachable authorized member device or cache-serving bot that already has a copy</li><li>cache-serving bots improve availability, but they do not become the source of truth for uploads</li></ul><p>It intentionally does <strong>not</strong> cover any virtual filesystem mapping (paths, directories, versioned mounts, etc.).</p><p>Terminology note:</p><ul><li>In repo discussions, a <strong>VDF</strong> is the <strong>cache-only peer role</strong> (most commonly <code>somad bot</code>).</li><li>In code, the crate is currently named <code>soma-vdfs</code> for historical reasons.</li><li>In user-facing docs, prefer <strong>cache-serving bot</strong> or <strong>cache peer</strong> over <code>VDF</code>.</li></ul><h2 id="goals" tabindex="-1">Goals <a class="header-anchor" href="#goals" aria-label="Permalink to &quot;Goals&quot;">​</a></h2><ul><li>Store binary assets (“blobs”: images, videos, files, editor attachments) <strong>out of band</strong> from collaborative document state.</li><li>Address blobs by a stable <strong>CID computed from bytes</strong> (content address).</li><li>Allow peers to <strong>fetch a blob by CID</strong> from any reachable authorized peer that has it (daemon store or bot cache).</li><li>Keep the networking surface <strong>pull‑based</strong> (no “push bytes to bot” protocol).</li></ul><h2 id="non‐goals" tabindex="-1">Non‑goals <a class="header-anchor" href="#non‐goals" aria-label="Permalink to &quot;Non‑goals&quot;">​</a></h2><ul><li>Virtual filesystem mapping / path semantics.</li><li>HTTP upload endpoints for bots (<code>somad bot</code> stays cache‑only).</li><li>Large file support beyond the current size-bounded request/response path.</li></ul><h2 id="concepts" tabindex="-1">Concepts <a class="header-anchor" href="#concepts" aria-label="Permalink to &quot;Concepts&quot;">​</a></h2><ul><li><strong>Blob</strong>: raw bytes + lightweight metadata (mime, name, size).</li><li><strong>CID</strong>: identity of a blob, computed from the blob’s bytes (today: SHA‑256 hex string).</li><li><strong>Space scope</strong>: blobs are stored under a <code>space_id</code> directory for layout and operational scoping.</li><li><strong>Daemon store vs bot cache</strong>: <ul><li>The desktop daemon library (<code>soma-daemon</code>, embedded in-process in the Tauri host via <code>desktop-daemon</code>) is the source of truth for user‑created blobs (uploaded through the upload command surfaced by <code>@soma/sdk</code>).</li><li><code>somad bot</code> is cache‑only for blobs (writes only as a side‑effect of fetching by CID).</li></ul></li></ul><h2 id="cid-format-today" tabindex="-1">CID format (today) <a class="header-anchor" href="#cid-format-today" aria-label="Permalink to &quot;CID format (today)&quot;">​</a></h2><ul><li>Algorithm: <strong>SHA‑256</strong></li><li>Encoding: <strong>lowercase hex</strong></li><li>Size: 64 chars (32 bytes)</li></ul><p>Implementations:</p><ul><li>Shared filesystem store: <code>soma_vdfs::fs::FsBlobStore</code> (<code>backend/crates/vdfs/src/fs.rs</code>) <ul><li>Used by both the in-process desktop daemon (authoritative store) and <code>somad bot</code> (cache-only by policy, populated via fetch).</li></ul></li></ul><p>Note: this is “CID” in the generic sense; it is not currently a multihash/CIDv1 string.</p><h2 id="storage-layout-filesystem" tabindex="-1">Storage layout (filesystem) <a class="header-anchor" href="#storage-layout-filesystem" aria-label="Permalink to &quot;Storage layout (filesystem)&quot;">​</a></h2><p>Both daemon and bot use the same layout:</p><p><code>&lt;blob_root&gt;/&lt;space_id&gt;/&lt;cid&gt;</code></p><p>Examples:</p><ul><li>Desktop daemon blob root: configured by the Tauri host&#39;s <code>desktop-daemon</code> runtime config; the desktop app places it under the app data dir (<code>~/Library/Application Support/Soma/</code> on macOS, <code>~/.local/share/soma/</code> on Linux).</li><li><code>somad bot</code> blob root: configured by <code>--blob-dir</code> / <code>SOMA_BLOB_DIR</code> (see <code>backend/bins/somad/src/commands/bot/</code>).</li></ul><h2 id="local-ingestion-desktop" tabindex="-1">Local ingestion (desktop) <a class="header-anchor" href="#local-ingestion-desktop" aria-label="Permalink to &quot;Local ingestion (desktop)&quot;">​</a></h2><p>Desktop UX stages blobs locally and then uploads them to the in-process daemon library:</p><ul><li>Renderer stages blobs locally and invokes an upload command via <code>@soma/sdk</code>.</li><li>The Tauri host (<code>desktop-commands</code> → <code>desktop-api</code>) receives the bytes.</li><li>Daemon persistence happens through the embedded daemon handle&#39;s <code>upload_blob</code>, after which the desktop renders daemon-owned blob references through <code>soma-blob://daemon/{space_id}/{cid}</code> (served by the host&#39;s <code>desktop-services::blob_protocol</code> handler).</li></ul><p>Daemon ingestion path:</p><ul><li>In-process method on the embedded daemon handle (<code>upload_blob</code>) — see <a href="./../../../backend/crates/daemon/src/handle/blobs.rs">DaemonHandle::upload_blob</a>.</li><li>Size limit: <code>MAX_UPLOAD_BYTES = 8 MiB</code> (enforced in the daemon handle).</li></ul><h2 id="network-fetch-protocol-libp2p" tabindex="-1">Network fetch protocol (libp2p) <a class="header-anchor" href="#network-fetch-protocol-libp2p" aria-label="Permalink to &quot;Network fetch protocol (libp2p)&quot;">​</a></h2><h3 id="protocol-id" tabindex="-1">Protocol id <a class="header-anchor" href="#protocol-id" aria-label="Permalink to &quot;Protocol id&quot;">​</a></h3><ul><li><code>/soma/blob/1</code> (see <code>BLOB_PROTOCOL</code> in <code>backend/crates/peer/src/lib.rs</code>)</li></ul><h3 id="transport" tabindex="-1">Transport <a class="header-anchor" href="#transport" aria-label="Permalink to &quot;Transport&quot;">​</a></h3><ul><li>libp2p request/response behaviour (<code>libp2p::request_response</code>)</li><li>Request timeout: 30s (<code>backend/crates/peer/src/lib.rs</code>)</li></ul><h3 id="messages" tabindex="-1">Messages <a class="header-anchor" href="#messages" aria-label="Permalink to &quot;Messages&quot;">​</a></h3><p>Defined as <code>prost</code> messages in <code>backend/crates/peer/src/lib.rs</code>:</p><ul><li><code>BlobRequest { cid: string, space_id: string }</code></li><li><code>BlobResponse { cid, mime, size, data, found, space_id }</code></li></ul><p>Framing and limits:</p><ul><li>Messages are encoded with <code>prost</code> and framed with a 4‑byte big‑endian length prefix.</li><li><code>MAX_BLOB_MESSAGE_BYTES = 8 MiB</code> bounds any single blob request/response message.</li></ul><p>This means blobs are currently limited to “small attachment” sizes.</p><h2 id="provider-boundary-blobprovider" tabindex="-1">Provider boundary (<code>BlobProvider</code>) <a class="header-anchor" href="#provider-boundary-blobprovider" aria-label="Permalink to &quot;Provider boundary (`BlobProvider`)&quot;">​</a></h2><p><code>soma-peer</code> treats “blob storage” as a dependency injected into the peer runtime:</p><ul><li>Trait: <code>soma_vdfs::BlobProvider</code> (<code>backend/crates/vdfs/src/lib.rs</code>) <ul><li><code>get(cid, space_id) -&gt; Option&lt;BlobResponse&gt;</code></li><li><code>put(expected_cid, space_id, bytes, mime) -&gt; SomaResult&lt;bool&gt;</code> (implementations verify CID before writing)</li></ul></li></ul><p>Implementations:</p><ul><li>Desktop daemon library and <code>somad bot</code>: <code>soma_vdfs::fs::FsBlobStore</code> (<code>backend/crates/vdfs/src/fs.rs</code>)</li></ul><p>Operational note: current filesystem implementations require a non‑empty <code>space_id</code> and will refuse to read/write if it is missing.</p><h2 id="fetch-flow-conceptual" tabindex="-1">Fetch flow (conceptual) <a class="header-anchor" href="#fetch-flow-conceptual" aria-label="Permalink to &quot;Fetch flow (conceptual)&quot;">​</a></h2>',46)),(o(),l(p,null,{default:t(()=>[c(s,{id:"mermaid-338",class:"mermaid",graph:"sequenceDiagram%0A%20%20autonumber%0A%20%20participant%20UI%20as%20Desktop%20UI%0A%20%20participant%20D%20as%20soma-daemon%20(in-process%20in%20the%20Tauri%20host%20%E2%80%94%20peer%20%2B%20blob%20store)%0A%20%20participant%20P%20as%20soma-peer%20runtime%20(libp2p)%0A%20%20participant%20R%20as%20Remote%20peer%20(daemon%20or%20bot)%0A%20%20participant%20S%20as%20Remote%20BlobProvider%20(store%2Fcache)%0A%0A%20%20UI-%3E%3ED%3A%20read%20blob%20by%20CID%0A%20%20D-%3E%3EP%3A%20PeerCommand%3A%3AFetchBlob(target%2C%20cid%2C%20space_id)%0A%20%20P-%3E%3ER%3A%20%2Fsoma%2Fblob%2F1%20BlobRequest(cid%2C%20space_id)%0A%20%20R-%3E%3ES%3A%20BlobProvider.get(cid%2C%20space_id)%0A%20%20S--%3E%3ER%3A%20bytes%20(or%20miss)%0A%20%20R--%3E%3EP%3A%20BlobResponse(found%3Dtrue%7Cfalse%2C%20data%3Dbytes)%0A%20%20P-%3E%3ED%3A%20BlobProvider.put(expected_cid%2C%20space_id%2C%20bytes)%0A"})]),fallback:t(()=>[...e[0]||(e[0]=[n(" Loading... ",-1)])]),_:1})),e[2]||(e[2]=a('<p>Today, the peer runtime already supports <code>/soma/blob/1</code> and <code>PeerCommand::FetchBlob</code>, while desktop rendering reads daemon-owned blobs through the Tauri host&#39;s <code>soma-blob://daemon/...</code> protocol handler.</p><h2 id="security-and-limits" tabindex="-1">Security and limits <a class="header-anchor" href="#security-and-limits" aria-label="Permalink to &quot;Security and limits&quot;">​</a></h2><ul><li>Always enforce a maximum blob size at ingress (the upload command) and egress (network transfer). Current limit is 8 MiB on both paths.</li><li>Always verify bytes match the CID before persisting or serving (both current FS implementations do this on <code>put</code>).</li><li>Treat remote blobs as untrusted: do not automatically execute or render without appropriate UI sandboxing.</li><li>Blob serving is membership-gated at the peer layer. Additional permission granularity may still evolve, but the fetch path is no longer intended to be open to non-members.</li></ul><h2 id="implementation-note-shared-fs-backend" tabindex="-1">Implementation note: shared FS backend <a class="header-anchor" href="#implementation-note-shared-fs-backend" aria-label="Permalink to &quot;Implementation note: shared FS backend&quot;">​</a></h2><p>The desktop daemon library and <code>somad bot</code> share a single filesystem backend in <code>soma-vdfs</code>:</p><ul><li><code>soma_vdfs::fs::FsBlobStore</code> (<code>backend/crates/vdfs/src/fs.rs</code>)</li></ul><p>Policy-level differences (&quot;authoritative store&quot; vs &quot;cache-only&quot;) are enforced by which code paths are exposed to users (the desktop upload command vs network pull-by-CID) rather than by separate storage implementations today.</p>',7))])}const k=r(b,[["render",h]]);export{_ as __pageData,k as default};
