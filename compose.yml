# Requires Docker Compose v2.20+ for `include`.
# If you're on an older version, use the multi-file invocation listed in README.md.
include:
  - compose/backend.infra.yml
  - compose/backend.botd.yml
  - compose/backend.bffd.yml
